Domain 1: Agentic Architecture & Orchestration (16 questions)
Q1: Your agent's agentic loop receives a response with stop_reason: "tool_use" containing two tool calls: get_customer and lookup_order. What is the correct execution pattern?
A) Execute get_customer, append result, call Claude, then execute lookup_order B) Execute both tools in parallel, collect all results, append as a single user message, continue loop C) Execute only get_customer since it should come first logically D) Ask Claude which tool to execute first
Answer: B - When Claude requests multiple tools in one turn, execute all and return results together in a single message. This enables parallelization and provides complete context for Claude's next reasoning step.
________________________________________
Q2: After processing a refund, your agent returns stop_reason: "end_turn" with the message "Refund processed successfully. Is there anything else I can help with?" What should your implementation do?
A) Continue the loop since the agent asked a question B) Terminate the loop and return the response to the user C) Automatically generate a follow-up query D) Call the agent again with an empty message
Answer: B - stop_reason: "end_turn" signals completion. Return the response to the user. The loop starts fresh when the user responds.
________________________________________
Q3: Your agent occasionally skips calling get_customer and directly calls lookup_order using the customer's stated name, leading to incorrect account access. Which solution provides deterministic enforcement?
A) Add few-shot examples showing correct sequence B) Enhance system prompt with bold, ALL CAPS instructions C) Implement a PreToolUse hook that blocks lookup_order and process_refund until get_customer has returned a verified ID D) Have the agent self-check before each tool call
Answer: C - Programmatic enforcement via hooks provides guarantees. Financial/security operations require deterministic control, not probabilistic compliance.
________________________________________
Q4: Your agentic loop appends tool results to conversation history. After 20 turns, you notice the context window is 90% full. Which architectural pattern best addresses this?
A) Switch to a model with 200k context window B) Trim verbose tool outputs to essential fields before adding to context, and extract key facts into a persistent case summary C) Restart the conversation every 10 turns D) Use compression on the conversation history
Answer: B - Context optimization through trimming unnecessary tool output fields and maintaining a structured fact summary prevents bloat while preserving critical information.
________________________________________
Q5: Your agent processes a customer request that involves three distinct issues: (1) refund for order #123, (2) account billing error, (3) update shipping address. The agent attempts to handle all simultaneously and produces a confusing response. What's the best architectural approach?
A) Increase max_tokens to allow longer, more detailed responses B) Decompose into separate issues, handle each in isolated context, then synthesize resolutions C) Add instructions to handle issues sequentially D) Create three separate agent instances
Answer: B - Multi-concern decomposition prevents context mixing. Process each issue independently, then unify responses. This matches the multi-agent task decomposition pattern.
________________________________________
Q6: You implement an agentic loop but notice it never terminates, continuing to call tools even after the customer's issue is resolved. What's the most likely issue?
A) The loop doesn't check for stop_reason: "end_turn" B) The model temperature is too high C) Tool results aren't being properly formatted D) The system prompt doesn't tell the agent when to stop
Answer: A - The loop must check stop_reason. When it's "end_turn", terminate and return response. When it's "tool_use", continue loop.
________________________________________
Q7: Your agent uses tools that can take 5-15 seconds to execute (database lookups, external API calls). During tool execution, you want to show the user progress updates like "Looking up your order..." How should you implement this?
A) Have Claude generate progress messages before each tool call B) Implement progress callbacks in your tool execution layer that emit status updates while waiting for tool results C) Use streaming API to show Claude's reasoning D) Reduce tool execution time so progress updates aren't needed
Answer: B - Progress updates are a presentation layer concern. Emit status messages during tool execution in your application code, not through the agent's reasoning loop.
________________________________________
Q8: When your agent calls a tool, the tool returns {"status": "error", "message": "Database timeout"} as plain text. The agent frequently misinterprets this and tells the customer their order doesn't exist. What's the issue?
A) The agent needs better error handling instructions B) Tool errors should use structured format with isError: true, errorCategory, and machine-parseable fields C) The database needs better performance D) Add retry logic in the agent's prompt
Answer: B - Structured error responses enable reliable error handling. Unstructured error messages are interpreted as regular content, causing misunderstanding.
________________________________________
Q9: You want to enforce that refunds over $500 require manager approval. Where should this logic be implemented?
A) In the system prompt with explicit instructions B) In a PostToolUse hook that intercepts process_refund calls and blocks amounts over $500 C) In the refund tool's description D) As a validation step after the agent completes
Answer: B - Business rules with compliance requirements need programmatic enforcement. Hooks provide deterministic control over tool execution.
________________________________________
Q10: Your agent handles both simple (password reset) and complex (multi-order dispute) requests. You notice simple requests take too long because the agent over-investigates. What's the best approach?
A) Create separate agents for simple vs complex requests with different tool sets B) Add instructions to handle simple requests quickly C) Reduce max_tokens for faster responses D) Use a faster model
Answer: A - Specialized agents with focused tool sets and appropriate instructions handle different complexity levels efficiently. A password reset agent shouldn't have access to financial tools.
________________________________________
Q11: Your agent's tool calls show this pattern: get_customer → Result → lookup_order → Result → lookup_order (same order again) → Result. Why might this redundant call happen?
A) The agent forgot the previous result due to context limits B) Tool results aren't being properly appended to conversation history C) The agent is validating the previous result D) This is normal behavior for verification
Answer: B - If tool results aren't appended correctly to messages, the agent doesn't see previous results and re-requests the same information.
________________________________________
Q12: You implement an agentic loop that checks stop_reason for loop control. Which additional stop_reason value must be handled?
A) "max_tokens" - when the response is truncated B) "stop_sequence" - when a custom stop sequence is triggered
C) "error" - when an API error occurs D) "thinking" - when the agent is reasoning
Answer: A - max_tokens indicates truncation. This should trigger error handling (possibly increasing max_tokens) rather than treating it as completion.
________________________________________
Q13: Your agent successfully resolves 85% of requests but has inconsistent response quality - some customers get detailed explanations, others get terse responses. What's the most likely architectural cause?
A) Varying context lengths affecting response quality B) Inconsistent system prompt application C) Model randomness (temperature setting) D) Different customer communication styles affecting the agent
Answer: A - As context grows during conversations, available generation space shrinks, leading to terser responses. Context management (trimming, summarization) ensures consistent response capacity.
________________________________________
Q14: You want your agent to handle a customer request that requires three sequential operations: verify identity → check eligibility → process refund. Each step depends on the previous step's result. How should the agent's tool execution flow?
A) The agent should call all three tools simultaneously for efficiency B) The agentic loop naturally handles this - the agent calls verify_identity, sees the result, then decides the next tool C) Pre-configure the tool sequence in the system prompt D) Use tool_choice to force the sequence
Answer: B - The agentic loop's design handles sequential dependencies naturally. After each tool execution, Claude reasons about the result and decides the next action.
________________________________________
Q15: Your agent handles escalations by calling the escalate_to_human tool. After escalation, what should the agentic loop do?
A) Continue attempting to resolve the issue B) Terminate the loop and transfer control to human handoff workflow C) Wait for human agent to respond through the same loop D) Restart the conversation
Answer: B - Escalation is a terminal action for the autonomous agent. The loop completes and control transfers to human agent workflow.
________________________________________
Q16: You're implementing conversation memory for your support agent so returning customers don't have to repeat their issue. How should this be architecturally integrated?
A) Load previous conversation history into the initial message context B) Store summaries in a database and retrieve them as tool results when the customer returns C) Use Claude's built-in conversation memory D) Ask the customer to summarize their previous issue
Answer: B - Conversation history should be retrieved via a tool (e.g., get_customer_history) that returns structured summaries. This keeps the pattern consistent and allows the agent to decide when to retrieve history.
________________________________________
Domain 2: Tool Design & MCP Integration (11 questions)
Q17: Your get_customer and lookup_order tools have similar descriptions: "Retrieves customer information" and "Retrieves order details". The agent frequently selects the wrong tool. What's the most effective fix?
A) Add few-shot examples showing correct tool selection B) Expand descriptions to include: specific use cases, input formats, output structure, edge cases, and explicit differentiation C) Rename the tools to be more distinct D) Use tool_choice to force correct selection
Answer: B - Rich tool descriptions are the primary mechanism for reliable tool selection. They should explicitly differentiate from similar tools and clarify when to use each.
________________________________________
Q18: Your process_refund tool can fail for multiple reasons: timeout, invalid customer ID, amount exceeds approval limit, insufficient account balance. How should errors be communicated?
A) Return HTTP status codes (400, 500, etc.) B) Return structured errors: {"isError": true, "errorCategory": "transient|validation|business|permission", "isRetryable": bool, "message": "...", "attemptedAction": "..."} C) Throw exceptions with error messages D) Return {"success": false, "error": "descriptive message"}
Answer: B - Structured errors with categorization enable intelligent agent recovery. Category indicates handling strategy; isRetryable guides retry logic; context enables informed recovery.
________________________________________
Q19: You're deciding how many tools to give your customer service agent. Currently it has 18 tools covering customer ops, orders, billing, shipping, inventory, and reporting. What principle should guide your decision?
A) More tools provide more capabilities, improving resolution rate B) Limit to 4-6 relevant tools per agent to reduce decision complexity and improve selection reliability C) Tool count doesn't affect performance if descriptions are clear D) Use all available tools but with very detailed descriptions
Answer: B - The exam guide explicitly states 18 tools degrades reliability. Specialized agents with focused tool sets (4-6 tools) maintain reliable tool selection.
________________________________________
Q20: Your MCP server for customer operations requires an API key. You're configuring it for your team. Where should the server be configured, and how should the API key be managed?
A) User-level ~/.claude.json with API key in the file B) Project-level .mcp.json with API key hardcoded C) Project-level .mcp.json with environment variable expansion ${CUSTOMER_API_KEY} D) Both project and user level for redundancy
Answer: C - Project-level configuration is shared via version control. Environment variables keep secrets out of version control while maintaining shared configuration.
________________________________________
Q21: Your get_customer tool returns 40+ fields (name, email, phone, address, purchase history, preferences, loyalty points, marketing consent, etc.). The agent only needs 5 fields for most operations. What's the optimization?
A) Keep the tool as-is since more information is better B) Create two tools: verify_customer (5 essential fields) and get_customer_details (complete profile) C) Add a parameter to specify which fields to return D) Compress the output to save tokens
Answer: B - Separate tools for different use cases optimizes context usage. Most operations need minimal data; comprehensive details only when necessary. Clear tool purposes improve selection.
________________________________________
Q22: You configure your customer service MCP server in .mcp.json. When team members clone the repository, what happens?
A) They must manually configure the MCP server in their environment B) The server is automatically available when they use Claude Code/Agent SDK in that project C) They must run an installation script D) Configuration must be copied to their ~/.claude.json
Answer: B - Project-level MCP configuration in version control is automatically discovered, making tools available to all team members without manual setup.
________________________________________
Q23: Your lookup_order tool description says "Retrieves order information". Your get_order_status tool says "Gets order status". The agent uses them interchangeably. What's missing from the descriptions?
A) Examples of when to use each tool B) Clear differentiation: "lookup_order returns complete order details (items, prices, shipping, history); get_order_status returns only current status (processing/shipped/delivered) for quick status checks" C) The input parameter formats D) The output structure
Answer: B - Tools with overlapping functionality need explicit boundary clarification. Specify not just what they do, but when to use each one.
________________________________________
Q24: You have three MCP servers: customer database, payment gateway, and inventory system. You want all three available to your agent. How do you configure this?
A) Three separate .mcp.json files B) One .mcp.json with three server entries in the mcpServers object C) Configure one server per terminal window D) Use @import to include multiple server configs
Answer: B - Single .mcp.json can define multiple servers. All are available simultaneously to the agent.
________________________________________
Q25: Your process_refund tool's description doesn't mention that it only works for orders less than 90 days old. The agent attempts refunds on old orders and gets errors. Where should this constraint be documented?
A) In the error message when it fails B) In the tool description: "Processes refunds for orders. Constraint: Orders must be less than 90 days old. For older orders, use escalate_to_human." C) In the system prompt D) As a separate validation tool
Answer: B - Tool descriptions should include constraints, edge cases, and boundary conditions so the agent knows when the tool is applicable.
________________________________________
Q26: You're designing a search_orders tool that can search by customer ID, order number, date range, or product. Should this be one tool or multiple?
A) One tool with parameters for different search modes B) Four separate tools: search_by_customer, search_by_order, search_by_date, search_by_product C) One tool with natural language query parsing D) Two tools: one for exact matches, one for fuzzy search
Answer: A - Related operations with the same conceptual purpose (searching orders) belong in one tool. Parameters specify search mode. Splitting would create tool selection overhead for a single logical operation.
________________________________________
Q27: Your agent needs to verify customer identity (frequently) and access detailed customer profiles (rarely). You initially had separate tools but merged them to reduce tool count. Now context fills quickly with unnecessary profile data. What's the correct architectural decision?
A) The merge was correct; optimize by compressing outputs B) Split back into verify_customer (minimal data) and get_customer_profile (comprehensive data); focused tools improve context efficiency C) Use a parameter to control output verbosity D) Keep merged tool but instruct agent to use it sparingly
Answer: B - This question reinforces the context optimization principle. Different use cases with different data needs warrant separate tools, even if it increases tool count slightly. The trade-off favors efficiency.
________________________________________
Domain 3: Claude Code Configuration & Workflows (12 questions)
Q28: Your customer support team uses Claude Code to draft response templates. You want to provide context about response tone, escalation criteria, and common issue resolutions. Where should this be documented?
A) In a shared Google Doc B) In .claude/CLAUDE.md in your support documentation repository C) In each team member's ~/.claude/CLAUDE.md D) In the Claude Code settings UI
Answer: B - Project-level CLAUDE.md provides shared context via version control. Team members automatically get the context when working in that repository.
________________________________________
Q29: A support agent has personal preferences for response style (they like adding emojis and casual language). These preferences should NOT apply to official customer support templates that the team uses. Where should they configure their personal style?
A) Project-level .claude/CLAUDE.md B) User-level ~/.claude/CLAUDE.md C) A separate style guide document D) In each request as a prompt
Answer: B - User-level configuration applies to all of that user's Claude Code sessions and overrides project settings for their personal work.
________________________________________
Q30: Your support team has response templates for different issue categories in templates/refunds/, templates/shipping/, templates/billing/. You want Claude to automatically apply category-specific guidelines when editing templates in each directory. What's the best approach?
A) Create .claude/rules/refunds.md, billing.md, etc. with glob patterns: paths: ["templates/refunds/**/*"] B) Create CLAUDE.md files in each template subdirectory C) Put all guidelines in root CLAUDE.md with section headers D) Create skills for each category
Answer: A - Path-specific rules with glob patterns automatically activate based on file location, making them ideal for directory-based conventions.
________________________________________
Q31: Your team shares customer support response guidelines across multiple projects (support portal, email templates, chat scripts). How should you structure this for reusability?
A) Copy-paste guidelines into each project's CLAUDE.md B) Create standards/support_guidelines.md and use @import standards/support_guidelines.md in each project's CLAUDE.md C) Use a monorepo with one CLAUDE.md D) Document in a wiki and ask team members to reference it
Answer: B - @import enables reusable standards. Guidelines are maintained in one place and imported where needed.
________________________________________
Q32: You create a custom command /check_policy that looks up company policies for customer issues. Where should this be located for team-wide availability?
A) .claude/commands/check_policy.sh in the repository B) ~/.claude/commands/check_policy.sh in each team member's home directory C) /usr/local/bin/check_policy.sh D) In a .claude/config.json file
Answer: A - Project-scoped commands in .claude/commands/ are shared via version control and automatically available to all team members.
________________________________________
Q33: Your support documentation repository has a CLAUDE.md at the root with response guidelines. A new team member reports Claude isn't applying the guidelines. What should you check first?
A) Whether they restarted Claude Code B) Whether they have a ~/.claude/CLAUDE.md with conflicting guidelines C) Whether the repository's .claude directory was committed to git D) Whether their Claude Code version is up to date
Answer: B - User-level configuration takes precedence. If they have personal guidelines that contradict project guidelines, their personal settings apply.
________________________________________
Q34: You're writing a CLAUDE.md for your support team. Which content is most appropriate?
A) Detailed Python SDK usage instructions B) Response tone guidelines, escalation criteria, common customer scenarios and resolutions, data privacy requirements C) Team member contact information D) Historical customer support metrics
Answer: B - CLAUDE.md should provide context relevant to the work: guidelines, standards, domain knowledge, and requirements specific to the project.
________________________________________
Q35: Your support team uses Claude Code in non-interactive mode to auto-generate response templates based on issue descriptions. Which flag is required?
A) --batch B) --non-interactive C) -p or --print D) --auto
Answer: C - --print (or -p) runs Claude Code in non-interactive mode, processing input and outputting results without waiting for user input.
________________________________________
Q36: Does Claude Code support user-level configuration that applies across all projects?
A) No, all configuration is project-specific B) Yes, via ~/.claude/CLAUDE.md and ~/.claude.json C) Yes, but only for MCP servers D) Yes, but only for slash commands
Answer: B - User-level configuration in the home directory applies globally to all of that user's Claude Code usage.
________________________________________
Q37: Your support team's CLAUDE.md has grown to 3000 lines with guidelines for refunds, shipping, billing, accounts, technical issues, and general communication. Team members report Claude's responses are inconsistent. What's the likely issue?
A) Claude Code has a CLAUDE.md size limit B) Long, unfocused context reduces effectiveness; split into focused modules and use @import, or use path-specific rules C) The content needs better formatting D) Team members need training
Answer: B - Overly long context dilutes focus. Modular organization with imports or path-specific rules provides relevant context without overwhelming.
________________________________________
Q38: Your team wants to provide customer service response templates that Claude can reference when drafting responses. These templates contain actual customer data for examples. Should these be in CLAUDE.md?
A) Yes, more examples improve quality B) No, CLAUDE.md may be committed to version control, exposing sensitive data; templates with real data should be in secure storage accessed via tools C) Yes, but with data redacted D) No, examples aren't useful in CLAUDE.md
Answer: B - Security consideration: CLAUDE.md is typically committed to version control. Sensitive data should not be included. Access via secure tools instead.
________________________________________
Q39: You want Claude Code to automatically apply your company's customer communication guidelines when working in any support-related repository. Where should these guidelines be configured?
A) In each repository's CLAUDE.md B) In user-level ~/.claude/CLAUDE.md for each support team member C) Create a shared guidelines file and use @import in each repository D) In a central documentation system
Answer: C - Shared guidelines file + @import balances reusability with project-specific integration. Option B requires manual setup per user.
________________________________________
Domain 4: Prompt Engineering & Structured Output (12 questions)
Q40: Your support agent generates inconsistent response formats - sometimes bullet points, sometimes paragraphs, sometimes numbered lists. You need consistent bullet-point format. What's the most reliable approach?
A) Add detailed formatting instructions to the system prompt B) Use a tool_use definition with JSON schema specifying response structure C) Provide 3-4 few-shot examples showing bullet-point responses D) Increase temperature for more consistent formatting
Answer: B - tool_use with JSON schema enforces structure at the API level, guaranteeing format compliance. Prompt instructions are probabilistic.
________________________________________
Q41: Your agent needs to extract structured information from customer emails: issue type, affected order number, requested resolution, and urgency level. How do you ensure consistent extraction format?
A) Detailed prompt instructions B) Define an extract_customer_issue tool with JSON schema defining the exact structure and required fields C) Few-shot examples D) Natural language parsing
Answer: B - JSON schema via tool_use guarantees structured output format. This is the recommended pattern for extraction tasks.
________________________________________
Q42: Your extraction schema has "urgency": {"type": "string", "enum": ["low", "medium", "high", "critical"]}. Some customer emails don't clearly indicate urgency. Should you make the field required?
A) Yes, the agent should always determine urgency B) No, make it "type": ["string", "null"] and remove from required fields to allow null when urgency isn't determinable C) Yes, but add "unknown" to the enum D) Use a default value of "medium"
Answer: B - Nullable optional fields prevent the agent from fabricating data when information isn't available in the source. This maintains extraction accuracy.
________________________________________
Q43: You implement extraction with JSON schema validation. Extraction succeeds (valid JSON) but semantic errors occur: order numbers don't match the format, dates are logically inconsistent. What's the issue?
A) JSON schema validation failed B) Schema validates syntax (types, format) but not semantics; additional validation logic is needed C) Temperature is too high D) The agent needs more examples
Answer: B - Critical distinction: schemas enforce format, not semantic correctness. Application logic must validate business rules (order number format, date logic, etc.).
________________________________________
Q44: After your extraction tool returns data, you validate it and find the customer name extracted doesn't appear anywhere in the email. How should you handle this?
A) Accept the extraction since it passed schema validation B) Implement a validation-retry loop: provide specific feedback ("Customer name 'John Smith' not found in email text") and request re-extraction C) Log the error and move on D) Lower confidence threshold
Answer: B - Validation-retry loops with specific error feedback enable self-correction. The agent learns what was wrong and can fix the specific issue.
________________________________________
Q45: Your agent generates customer responses but quality varies significantly - some responses are thorough and empathetic, others are terse and robotic. What technique most reliably improves consistency?
A) Increase max_tokens B) Provide 2-4 few-shot examples showing ideal customer responses with appropriate tone, structure, and empathy C) Add more detailed tone instructions D) Adjust temperature
Answer: B - Few-shot examples demonstrate desired output patterns (tone, structure, style) more effectively than descriptive instructions.
________________________________________
Q46: You're building a response quality classifier that rates agent responses (1-5 stars). Ratings are inconsistent - similar responses get different scores. How do you improve rating consistency?
A) Average multiple ratings B) Provide few-shot examples with concrete criteria: "5 stars: addresses all concerns + empathetic + clear next steps [example]. 3 stars: addresses concern but lacks empathy [example]" C) Use a different model D) Simplify to binary (good/bad)
Answer: B - Explicit criteria with concrete examples establish consistent evaluation standards. Rating inconsistency stems from ambiguous criteria.
________________________________________
Q47: Your support team processes 500 customer emails overnight into structured support tickets. Processing doesn't need to complete within minutes. What's the optimal approach?
A) Synchronous Messages API calls B) Message Batches API for 50% cost savings with up to 24-hour processing window C) Parallel processing with multiple API clients D) Stream API for faster response
Answer: B - Message Batches API is designed for non-blocking, latency-tolerant batch workloads. 50% cost savings for overnight processing is optimal.
________________________________________
Q48: You submit 500 emails to Message Batches API. After processing, 15 failed: 10 timeouts, 5 validation errors. What's the appropriate retry strategy?
A) Retry all 15 with identical input B) Retry the 10 timeout errors (transient); analyze and fix input for the 5 validation errors before retrying C) Discard failures and process the 485 successful ones D) Resubmit the entire batch
Answer: B - Error-aware retry: transient errors warrant immediate retry; validation errors need input correction. Don't reprocess successful items.
________________________________________
Q49: Your agent's escalation decisions are inconsistent - escalating simple password resets while attempting complex multi-order disputes. How do you improve escalation calibration?
A) Add explicit escalation criteria with few-shot examples: "Escalate when: [criteria]. Example of simple (don't escalate): [example]. Example of complex (escalate): [example]" B) Use confidence scoring C) Implement a separate escalation classifier D) Always escalate if the customer sounds frustrated
Answer: A - Explicit criteria with concrete examples establish consistent decision boundaries. Ambiguous instructions lead to inconsistent behavior.
________________________________________
Q50: Your customer email extraction works well for standard formats but fails on forwarded emails with nested quotes and inline replies. What technique addresses format variation?
A) Create separate extraction tools for each email format B) Add 2-3 few-shot examples showing correct extraction from varied formats (standard, forwarded, inline replies) C) Detailed instructions describing each format D) Preprocess emails to normalize format
Answer: B - Few-shot examples are specifically effective for handling format variations. Demonstrate the extraction pattern across different structures.
________________________________________
Q51: Your response generator occasionally includes placeholder text like "[customer name]" or "[order number]" instead of actual values. What's the most likely cause and fix?
A) The agent is lazy; add instructions to always fill placeholders B) Required information isn't in context; ensure all necessary data (customer name, order number) is provided in the prompt before generation C) Temperature is too low D) The model doesn't understand placeholders
Answer: B - Placeholders indicate missing information in context. Ensure complete data is provided before generation, or make fields optional in the response structure.
________________________________________
Q52: You want your agent to determine sentiment (positive/negative/neutral) from customer emails. Which approach is most reliable?
A) Natural language instructions: "Classify the sentiment..." B) Define a classify_sentiment tool with JSON schema: {"sentiment": {"type": "string", "enum": ["positive", "negative", "neutral"]}, "confidence": {"type": "number"}} C) Few-shot examples only D) Use keyword matching
Answer: B - Structured classification via tool_use with enum ensures valid output. Including confidence enables downstream routing decisions.
________________________________________
Domain 5: Context Management & Reliability (9 questions)
Q53: Your customer service conversations average 15-20 turns. By turn 15, you notice the agent gives generic responses like "your previous order" instead of specific order numbers mentioned in turn 4. What's the issue?
A) The model's memory capacity is limited B) Lost-in-the-middle effect causes detail loss from middle turns C) Tool results aren't being appended correctly D) The context window is full
Answer: B - "Lost in the middle" is a known issue where information from middle sections of long contexts is deprioritized. Solutions include position-aware structuring and fact extraction.
________________________________________
Q54: What architectural pattern best preserves critical facts (customer ID, order numbers, dates, amounts) across long conversations?
A) Increase context window size B) Extract key facts into a persistent "case facts" structure included at the start of every prompt C) Summarize conversation every 5 turns D) Use a vector database
Answer: B - Structured fact extraction creates a dedicated facts block that persists across turns, immune to summarization loss and position effects.
________________________________________
Q55: Your lookup_order tool returns 40+ fields per order (items, prices, shipping address, billing address, payment method, order history, tracking, delivery confirmations, etc.). Most operations need only 5-7 fields. What's the optimization?
A) Use a larger context window B) Trim tool output to relevant fields before adding to context C) Compress the entire output D) Use the full output but summarize it
Answer: B - Return only necessary fields from tools. If only 7 of 40 fields are needed, return only those 7. This respects token budgets without losing needed information.
________________________________________
Q56: Which scenario most appropriately triggers escalation to human agent?
A) Customer message contains negative sentiment words B) Customer explicitly requests to speak with a manager C) The agent's confidence score is below 0.7 D) The request involves more than two orders
Answer: B - Explicit customer requests for human escalation should be honored immediately. Sentiment, confidence, and complexity are unreliable proxies.
________________________________________
Q57: Your agent attempted to resolve a billing issue three times: (1) standard credit failed (policy limit), (2) manager override failed (manager unavailable), (3) alternative resolution failed (same policy limit). What should happen?
A) Try a fourth approach B) Escalate - the agent is not making progress despite multiple resolution attempts C) Ask customer to try again tomorrow D) Reduce the resolution amount
Answer: B - After multiple attempts without progress (hitting systemic barriers), escalation is appropriate. Continued attempts won't change the outcome.
________________________________________
Q58: In your support agent, the get_customer tool times out (network issue). It returns structured error: {"isError": true, "errorCategory": "transient", "isRetryable": true, "partialResults": {...}}. What should the agent do?
A) Immediately escalate since an error occurred B) Retry the operation, potentially with backoff, and use partial results if available C) Return an error message to the customer D) Try a different tool
Answer: B - Transient errors with isRetryable: true signal appropriate retry conditions. Partial results may contain useful information to proceed with.
________________________________________
Q59: Your support agent achieves 94% accuracy overall. However, when analyzed by issue type: password reset (99%), refund requests (96%), billing disputes (78%), shipping issues (92%). You use a global confidence threshold of 0.85 for human review routing. What's the issue?
A) The threshold is correct B) A single threshold doesn't account for performance variation by issue type; use issue-specific thresholds calibrated on validation data C) The model needs fine-tuning D) 78% accuracy on billing disputes is acceptable
Answer: B - Aggregate metrics mask category-specific performance. Stratified analysis and category-specific thresholds improve routing precision.
________________________________________
Q60: Your agent escalates issues to human agents. The human agent receives customer ID and issue type, but doesn't have access to the conversation history or what the agent already attempted. What information should the escalation handoff include?
A) Complete conversation transcript B) Structured summary: customer details, issue description, root cause analysis, attempted resolutions with results, relevant order/account data, recommended next actions C) Just customer ID - human agent will investigate D) Error logs from failed resolution attempts
Answer: B - Structured handoff provides essential context without overwhelming. Human agent gets actionable information: what was tried, what failed, what's recommended, relevant data.
________________________________________
________________________________________
SCENARIO 2: Code Generation with Claude Code (60 Questions)
You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and plan mode for complex refactorings.
________________________________________
Domain 1: Agentic Architecture & Orchestration (16 questions)
Q61: When using Claude Code's built-in agentic loop, which tool does Claude use to read the full contents of a source file?
A) Grep B) Read C) Edit D) Glob
Answer: B - Read tool retrieves complete file contents. Grep searches content, Edit modifies, Glob matches paths.
________________________________________
Q62: Claude Code attempts to modify a file using Edit with anchor text "timeout = 30". The Edit fails because this text appears in 3 files. What's the documented fallback pattern?
A) Use Bash with sed to replace the text B) Read the target file (get full content) → modify the text → Write the complete file back C) Use Edit with more context D) Ask the user which file to modify
Answer: B - Read → modify → Write is the fallback when Edit's anchor text is non-unique. This is documented in built-in tool usage patterns.
________________________________________
Q63: A developer asks Claude Code to "add comprehensive error handling to the authentication module" (30 files). What should Claude do?
A) Direct execution: start modifying files immediately B) Plan mode: analyze current error handling, identify gaps, propose strategy, get approval C) Edit each file sequentially D) Ask for detailed specifications
Answer: B - Plan mode for complex, multi-file architectural changes. This allows design validation before implementation.
________________________________________
Q64: Claude Code is working on a large refactoring task. Which mode prevents costly mistakes by allowing design review before implementation?
A) Debug mode B) Plan mode C) Safe mode D) Review mode
Answer: B - Plan mode enables exploration, design, and approval before committing to changes.
________________________________________
Q65: When should Claude Code use direct execution instead of plan mode?
A) Large-scale architectural changes B) Single-file bug fixes with clear scope C) Refactoring affecting 10+ files D) Exploring unfamiliar codebases
Answer: B - Direct execution for well-scoped, clear tasks (single file, obvious fix, following established patterns).
________________________________________
Q66: A developer asks "Find all callers of the authenticateUser function across the codebase." Which built-in tool should Claude use first?
A) Glob (to find files) B) Read (to understand the function) C) Grep (to search for function calls) D) Bash (to run a search command)
Answer: C - Grep searches content across files for patterns (like function calls). This is the primary tool for code search.
________________________________________
Q67: Claude Code needs to find all test files in a codebase. Which tool is most appropriate?
A) Grep B) Read
C) Glob with pattern **/*.test.ts D) Bash with find command
Answer: C - Glob matches file paths by pattern. For finding files by naming convention, Glob is the built-in tool of choice.
________________________________________
Q68: Claude Code is analyzing a large codebase (200+ files). To avoid reading unnecessary files, what's the efficient approach?
A) Read all files upfront B) Use Grep/Glob to find entry points, then Read incrementally following imports C) Use Bash to run static analysis D) Use Edit to add comments then read those
Answer: B - Incremental exploration: find entry points first (Grep for exports, main), then follow dependency chains (Read specific files). Avoids wasting context.
________________________________________
Q69: Claude Code's agentic loop uses built-in tools (Read, Write, Edit, Bash, Grep, Glob). Can these tools be restricted or customized?
A) No, built-in tools are always available B) Yes, via skill frontmatter allowed-tools configuration C) Yes, via CLAUDE.md tool restrictions D) No, but custom tools can override them
Answer: B - Skill frontmatter can specify allowed-tools to restrict which built-in tools the skill can use.
________________________________________
Q70: During code generation, Claude Code makes multiple file modifications across a feature implementation. How does the agentic loop structure these modifications?
A) All changes in a single Edit tool call B) Sequential tool calls: Read → Edit for each file, with Claude reasoning between modifications C) Parallel Write operations for all files D) Batch all changes then apply simultaneously
Answer: B - The agentic loop naturally sequences operations: read context, reason, modify one file, read next context, reason, modify next file, etc.
________________________________________
Q71: A developer invokes a Claude Code skill with context: fork for codebase analysis. What happens to the verbose analysis output?
A) It appears in the main conversation, consuming context B) It runs in isolated context; only a summary returns to the main conversation C) It's written to a log file D) It's discarded
Answer: B - context: fork isolates execution. Verbose output stays in the fork; only relevant summaries return to main context.
________________________________________
Q72: Claude Code encounters a bug it can't fix after 3 different attempted solutions. What's the appropriate escalation?
A) Keep trying more solutions B) Present the findings to the developer: what was tried, what failed, diagnostic information, suggested next steps C) Restart from scratch D) Skip the bug and move to next task
Answer: B - After exhausting reasonable attempts, escalate to developer with context: attempts, failures, diagnostics, suggestions. This is effective human-AI collaboration.
________________________________________
Q73: When using Claude Code's built-in tools, which tool executes arbitrary commands like running tests or build scripts?
A) Execute B) Bash C) Command D) Shell
Answer: B - Bash tool executes command-line operations (run tests, build, install packages, etc.).
________________________________________
Q74: A developer asks Claude Code to understand a complex module with circular dependencies. What's the efficient exploration strategy?
A) Read all files in the module B) Use Grep to map exports/imports, identify circular dependencies, then Read key files to understand the pattern C) Use Bash to run a dependency analyzer D) Edit files to add clarifying comments
Answer: B - Efficient pattern: map structure first (Grep for imports/exports), identify issues, then deep-dive (Read) on relevant files.
________________________________________
Q75: Claude Code is refactoring a module. After making changes, how should it verify the refactoring didn't break functionality?
A) Read the modified files to check syntax B) Use Bash to run the test suite C) Use Edit to add logging D) Use Grep to verify no errors appear in code
Answer: B - Running tests (via Bash) is the verification mechanism for functional correctness after refactoring.
________________________________________
Q76: A developer asks Claude Code to "optimize the performance of the dashboard module." This is ambiguous and could involve database queries, frontend rendering, caching, or algorithm efficiency. What should Claude do?
A) Start optimizing database queries (most common issue) B) Enter plan mode: analyze performance bottlenecks, identify optimization opportunities, propose prioritized approach C) Ask the developer to specify which aspect to optimize D) Optimize everything simultaneously
Answer: B - Ambiguous, multi-faceted tasks require exploration (plan mode) to identify actual issues and prioritize solutions.
________________________________________
Domain 2: Tool Design & MCP Integration (11 questions)
Q77: You're creating an MCP server that provides tools for your team's internal API documentation. Should this be configured at project or user level?
A) Project level (.mcp.json) so all team members have access B) User level (~/.claude.json) for personal access C) Both levels for redundancy D) System level for global access
Answer: A - Team-shared tools belong in project-level configuration, shared via version control.
________________________________________
Q78: Your MCP server for code analysis provides these tools: analyze_complexity, check_security, measure_coverage, profile_performance, check_style, find_duplicates. You want different agents for different purposes (code review agent, security audit agent). How should tools be distributed?
A) All agents get all tools B) Code review agent: complexity, coverage, style, duplicates. Security audit agent: security, style. Performance agent: complexity, profile_performance C) Each agent gets one tool D) Use tool_choice to restrict access
Answer: B - Distribute tools based on agent role. Focused tool sets improve selection reliability and maintain separation of concerns.
________________________________________
Q79: Your MCP tool run_tests can fail for various reasons: tests failed, syntax error, timeout, missing dependencies. How should errors be structured?
A) Return exit code and stdout/stderr B) Structured error: {"isError": true, "errorCategory": "test_failure|syntax|transient|environment", "failedTests": [...], "isRetryable": bool, "diagnostics": "..."} C) Throw exception D) Return simple error string
Answer: B - Structured errors enable intelligent handling. Distinguish test failures (need fixing) from environment issues (need setup) from transient failures (retry).
________________________________________
Q80: You're integrating an MCP server that requires authentication tokens. The tokens are team-specific but each developer should use their own. How should this be configured?
A) Hardcode tokens in project .mcp.json B) Project .mcp.json defines the server with env variable ${DEV_TOKEN}; each developer sets their token in environment C) Each developer configures the server in ~/.claude.json with their token D) Store tokens in a shared keychain
Answer: B - Project config with environment variables: shared configuration, individual credentials.
________________________________________
Q81: Your codebase MCP server provides tools: get_file, search_code, get_dependencies, run_tests, deploy_to_staging. A code review agent should never deploy. How do you enforce this?
A) Trust the agent not to deploy B) The review agent's tool list shouldn't include deploy_to_staging C) Add instructions not to deploy D) Use tool_choice to block deployment
Answer: B - Tool distribution: only provide tools appropriate to the agent's role. Review agents shouldn't even see deployment tools.
________________________________________
Q82: Your MCP tool descriptions for analyze_file and review_code are both "Analyzes code." Claude frequently selects the wrong tool. What's the fix?
A) Rename the tools B) Expand descriptions: "analyze_file: Performs static analysis (complexity, maintainability metrics, code smells). Use for objective code quality metrics. | review_code: Performs subjective code review (design patterns, readability, best practices). Use for human-focused feedback." C) Add few-shot examples D) Consolidate into one tool
Answer: B - Clear differentiation in descriptions: what each does, when to use it, how they differ. This directly addresses selection ambiguity.
________________________________________
Q83: You configure three MCP servers (GitHub, Jira, Internal API) in .mcp.json. When Claude Code starts in this project, what happens?
A) Only the first server is available B) Claude asks which server to use C) All three servers' tools are available simultaneously D) Servers must be manually activated
Answer: C - All configured MCP servers are discovered at startup and their tools are available concurrently.
________________________________________
Q84: Your MCP tool refactor_code takes a file path and refactoring type as parameters. It returns the refactored code. Should this be one tool or separate tools per refactoring type?
A) One tool with a refactoring_type parameter (extract_method, rename_variable, inline_function, etc.) B) Separate tools: extract_method, rename_variable, inline_function C) Two tools: one for extractions, one for renamings D) Use the Edit built-in tool instead
Answer: A - Related operations with the same conceptual action (refactoring) belong in one tool with parameters specifying the operation type.
________________________________________
Q85: Your MCP tool get_test_coverage returns detailed per-file, per-function coverage data (hundreds of lines). The agent typically only needs overall coverage percentage and files with <80% coverage. What's the optimization?
A) Keep verbose output for completeness B) Create two tools: get_coverage_summary (overall + low-coverage files) and get_coverage_details (comprehensive data) C) Compress the output D) Return summary by default with a verbose flag
Answer: B - Separate tools for different detail levels optimizes context usage. Most operations need summary; comprehensive data only for deep analysis.
________________________________________
Q86: Your development MCP server uses a personal API key for GitHub access. Where should this be configured so it doesn't affect team members?
A) Project .mcp.json B) User ~/.claude.json C) Environment variable in project D) In CLAUDE.md
Answer: B - Personal integrations (using personal API keys) belong in user-level configuration.
________________________________________
Q87: Your MCP tool deploy_to_staging should only be callable from the main branch, not from feature branches. Where should this enforcement be?
A) In the tool implementation (check current branch) B) In the tool description (tell Claude not to deploy from feature branches) C) In CLAUDE.md instructions D) In a PreToolUse hook that checks branch
Answer: A - Business rules with security implications need programmatic enforcement in the tool implementation itself.
________________________________________
Domain 3: Claude Code Configuration & Workflows (12 questions)
Q88: Your codebase has different conventions: React components (functional + hooks), API routes (async/await patterns), database models (repository pattern), tests (Jest conventions). You want Claude to automatically apply the right conventions based on file location. What's the optimal configuration?
A) Put all conventions in root CLAUDE.md with section headers B) Create .claude/rules/ files with path patterns: react.md with paths: ["src/components/**/*.tsx"], api.md with paths: ["src/api/**/*"], etc. C) Create CLAUDE.md in each directory D) Create skills for each type
Answer: B - Path-specific rules automatically activate based on file location. Glob patterns match files across directories (e.g., tests spread throughout codebase).
________________________________________
Q89: Your team's Python style guide is shared across 5 microservice repositories. How should this be configured for maintainability?
A) Copy the style guide into each repo's CLAUDE.md B) Create standards/python_style.md and use @import standards/python_style.md in each repo's CLAUDE.md C) Put style guide in team wiki D) Configure in each developer's ~/.claude/CLAUDE.md
Answer: B - @import enables reusable standards. Single source of truth, imported where needed.
________________________________________
Q90: You create a custom command /run-checks that runs linting, tests, and security scans. Where should this be located for team-wide availability?
A) .claude/commands/run-checks.sh in the repository B) ~/.claude/commands/run-checks.sh in home directories C) /usr/local/bin/run-checks.sh D) As a skill instead of a command
Answer: A - Project-scoped commands in .claude/commands/ are version-controlled and automatically available to all team members.
________________________________________
Q91: Your project has CLAUDE.md at the root with general coding standards. You also have tests/CLAUDE.md with testing conventions. When editing tests/integration/api.test.ts, which configuration(s) apply?
A) Only tests/CLAUDE.md B) Only root CLAUDE.md C) Both, with tests/CLAUDE.md having precedence for conflicting guidelines D) Neither, directory-level only
Answer: C - Configuration hierarchy: more specific (directory-level) overrides general (root), but both apply with directory-level taking precedence for conflicts.
________________________________________
Q92: You're onboarding a new team member. They clone the repository which has a configured .claude/ directory with CLAUDE.md, rules, commands, and MCP config. What setup is required for them to use these configurations?
A) Manual configuration copying B) Nothing - configurations are automatically available when working in the repository C) Run an initialization script D) Configure their ~/.claude.json with the project settings
Answer: B - Project-level configurations in version control are automatically discovered by Claude Code. No manual setup needed.
________________________________________
Q93: Your CLAUDE.md includes sensitive API endpoints and internal architecture details. Is this appropriate?
A) Yes, more context improves code generation B) It depends - CLAUDE.md may be committed to version control; sensitive information should be in secure documentation accessed via tools or environment variables C) No, CLAUDE.md should only contain style guidelines D) Yes, CLAUDE.md is always private
Answer: B - Security consideration: CLAUDE.md is typically version-controlled. Sensitive information should be externalized or accessed through secure tools.
________________________________________
Q94: You create a skill for analyzing code complexity with this frontmatter:
yaml
context: fork
allowed-tools: [Read, Grep, Glob]
Why is context: fork beneficial here?
A) It makes the skill run faster B) It isolates verbose analysis output from the main conversation, preventing context pollution C) It enables parallel execution D) It provides access to more tools
Answer: B - Fork context isolates verbose/exploratory work. The skill can read many files and generate detailed analysis without polluting the main conversation context.
________________________________________
Q95: Your codebase has test files located in multiple places: tests/ directory, and co-located *.test.ts files alongside source code. You want testing conventions to apply to all tests. How do you configure this?
A) Create tests/CLAUDE.md (only covers tests/ directory) B) Create .claude/rules/testing.md with paths: ["**/*.test.ts", "**/*.spec.ts", "tests/**/*"] C) Put testing conventions in root CLAUDE.md D) Create a testing skill
Answer: B - Glob patterns in rules files match files regardless of location. This applies conventions to tests anywhere in the codebase.
________________________________________
Q96: A developer has personal preferences (verbose variable names, explicit type annotations) that differ from project standards. Where should they configure their preferences?
A) Project CLAUDE.md B) User ~/.claude/CLAUDE.md C) In commit messages D) They shouldn't - must follow project standards
Answer: B - User-level configuration for personal preferences. However, note that project standards typically should take precedence for team code.
________________________________________
Q97: Your repository's CLAUDE.md has grown to 2500 lines covering architecture, conventions, patterns, deployment, testing, security guidelines. Claude's responses are becoming inconsistent. What's the likely issue?
A) CLAUDE.md is too long B) Large, unfocused context reduces effectiveness; split into focused modules with @import or path-specific rules C) Need better formatting D) Claude Code has a size limit
Answer: B - Overly long context dilutes focus and effectiveness. Modular organization provides relevant context without overwhelming.
________________________________________
Q98: You want to provide boilerplate code examples for common patterns (API endpoints, React components, test structure) that Claude can reference. Where should these go?
A) In CLAUDE.md as examples B) As separate example files in docs/examples/ referenced in CLAUDE.md C) In skills D) In custom commands
Answer: B - Extensive code examples should be in separate files to keep CLAUDE.md focused. Reference them in CLAUDE.md for discoverability.
________________________________________
Q99: You configure an MCP server in .mcp.json with environment variable ${API_KEY}. A team member reports the server isn't working. What should you verify?
A) That they restarted Claude Code B) That they have the API_KEY environment variable set in their development environment C) That the .mcp.json file was committed D) That their Claude Code version supports MCP
Answer: B - Environment variable expansion requires the variable to be set in the user's environment. The config references it, but users must provide values.
________________________________________
Domain 4: Prompt Engineering & Structured Output (12 questions)
Q100: Claude Code generates code with inconsistent formatting - sometimes tabs, sometimes spaces, inconsistent bracket placement. What's the most reliable solution?
A) Detailed formatting instructions in CLAUDE.md B) Use a generate_code tool with JSON schema enforcing structure C) Few-shot examples showing desired formatting D) Run a code formatter (like prettier/black) after generation via Bash tool
Answer: D - Code formatting is best handled by dedicated formatters. Generate code (content focus), then format (style focus). Separates concerns and leverages specialized tools.
________________________________________
Q101: You want Claude Code to extract structured information from code comments (TODO items with priority, owner, description). How do you ensure consistent structure?
A) Natural language instructions B) Define an extract_todos tool with JSON schema: [{"priority": "high|medium|low", "owner": "string", "description": "string", "file": "string", "line": number}] C) Few-shot examples D) Use Grep with pattern matching
Answer: B - JSON schema via tool_use guarantees structured extraction format. Ensures consistency across codebase scans.
________________________________________
Q102: Your code review tool rates code quality (1-5 stars) but ratings are inconsistent - similar code gets different ratings. What improves consistency?
A) Average multiple ratings B) Few-shot examples with explicit criteria: "5: Clean, tested, documented [example]. 3: Works but needs refactoring [example]. 1: Critical issues [example]" C) Simplify to pass/fail D) Use static analysis instead
Answer: B - Explicit criteria with concrete examples establish consistent rating standards. Shows what each rating means with real code examples.
________________________________________
Q103: Claude Code generates 50 unit tests overnight for new features. Tests need consistent structure (arrange-act-assert) and naming (should_behavior_when_condition). What ensures consistency?
A) Detailed instructions in CLAUDE.md B) 2-3 few-shot examples in CLAUDE.md showing ideal test structure, naming, and organization C) A test template file D) Post-generation validation
Answer: B - Few-shot examples demonstrate patterns (structure, naming, style) more effectively than descriptive instructions. Show, don't just tell.
________________________________________
Q104: You're generating API documentation from code. Source code varies: some functions have detailed docstrings, others have minimal comments, some have none. You want consistent documentation output format. What's the approach?
A) Detailed instructions describing the format B) Define a generate_documentation tool with JSON schema specifying documentation structure (summary, parameters, returns, examples, errors) C) Few-shot examples only D) Skip functions without docstrings
Answer: B - JSON schema enforces output structure consistency regardless of input variation. Even minimal source comments can be structured into consistent documentation format.
________________________________________
Q105: Claude Code reviews a pull request with 12 files. You notice superficial review of some files and missed issues that appear in multiple files. What's the architectural solution?
A) Use a larger model B) Multi-pass review: per-file local analysis pass, then separate cross-file integration/consistency pass C) More detailed review instructions D) Review fewer files at once
Answer: B - Multi-pass architecture: focused per-file passes ensure consistent depth, separate integration pass catches cross-file issues (duplicated logic, inconsistent patterns).
________________________________________
Q106: Your code generation includes error handling, but the quality varies - sometimes comprehensive try-catch with specific error types, sometimes bare try-catch with generic handling. What improves consistency?
A) Strict instructions to always use specific error handling B) 2-4 few-shot examples showing error handling patterns for different scenarios (API calls, file operations, validation) C) A linting rule D) Post-generation error handling injection
Answer: B - Few-shot examples demonstrate error handling patterns across different contexts, showing how to handle specific error types appropriately.
________________________________________
Q107: You're generating migration scripts for database schema changes. Errors could be catastrophic. How do you ensure generated migrations are safe?
A) Trust Claude Code's generation B) Generate migration in plan mode with validation: analyze schema, identify changes, generate migration, show preview, explain risks, require approval C) Always manually review after generation D) Use a migration validation tool
Answer: B - Critical operations warrant plan mode: design review before execution. Migrations should be previewed and explained before being applied.
________________________________________
Q108: Claude Code generates code comments, but style varies: some are detailed multi-line explanations, some are terse single lines. You need consistent JSDoc format. What's the approach?
A) Instructions specifying JSDoc format B) Few-shot examples showing JSDoc format for functions, classes, and methods C) Use a documentation generator instead D) Post-process with a formatter
Answer: B - Few-shot examples demonstrate the documentation pattern (format, detail level, structure) more effectively than format specifications.
________________________________________
Q109: You want Claude Code to identify code smells during review: long functions, deep nesting, duplicated logic, magic numbers. Output should be structured for automated tracking. What's the approach?
A) Natural language review B) Define a find_code_smells tool with JSON schema: [{"type": "long_function|deep_nesting|duplication|magic_number", "file": "string", "line": number, "severity": "high|medium|low", "suggestion": "string"}] C) Use static analysis tools via Bash D) Few-shot examples only
Answer: B - Structured output via JSON schema enables automated tracking, aggregation, and trend analysis. Consistent format for reporting and metrics.
________________________________________
Q110: Claude Code generates test cases but some tests are flaky (pass/fail inconsistently). You want the generator to identify potential flakiness sources (timing dependencies, shared state, external services). What technique helps?
A) Instructions to avoid flaky patterns B) Few-shot examples showing flaky vs robust test patterns with explanations of what causes flakiness C) Generate more tests D) Run tests multiple times
Answer: B - Few-shot examples demonstrating flaky patterns vs robust alternatives educate the generator about what to avoid and why.
________________________________________
Q111: You're generating boilerplate code for new API endpoints (route handler, validation, error handling, logging). Consistency across endpoints is critical. What ensures consistency?
A) Detailed template documentation B) Few-shot examples showing complete endpoint implementation following the pattern C) A code generation tool D) Manual review
Answer: B - Few-shot examples of complete patterns (showing how all pieces fit together) are most effective for consistent boilerplate generation.
________________________________________
Domain 5: Context Management & Reliability (9 questions)
Q112: Claude Code is working on a large refactoring (30+ files). By file 20, you notice it refers to "the earlier changes" generically instead of specifically. What's the issue?
A) Memory capacity exceeded B) Lost-in-the-middle effect or progressive summarization losing detail from earlier files C) Context window is full D) The agent is being lazy
Answer: B - Long sequences cause detail loss from middle sections. Earlier work (files 1-15) loses specificity by the time later files are processed.
________________________________________
Q113: During a multi-file refactoring, what pattern preserves critical refactoring decisions (naming conventions, architectural patterns, migration strategy) across the entire process?
A) Repeat instructions in each prompt B) Extract refactoring decisions into a structured "refactoring plan" block maintained at the start of context throughout the session C) Use a larger context window D) Process fewer files at once
Answer: B - Structured decision/plan extraction creates a persistent reference that doesn't degrade through summarization or position effects.
________________________________________
Q114: Claude Code's get_file_contents tool returns complete file contents including comments, imports, and all functions. For many operations, only the function signature and doc is needed. What's the optimization?
A) Always use full contents B) Create two tools: get_file_info (signatures, exports, imports) and get_file_contents (complete content) C) Compress file contents D) Strip comments automatically
Answer: B - Different detail levels for different needs. File info (structure) for navigation/understanding; full contents for detailed work. Optimizes context usage.
________________________________________
Q115: Claude Code is analyzing a codebase with 150 files. Reading all files would exceed context limits. What's the efficient strategy?
A) Use a 200k context model B) Entry point exploration: Grep for exports/main → Read entry files → follow imports incrementally → targeted deep-dives C) Read summaries of all files D) Process in batches of 50 files
Answer: B - Incremental exploration starting from entry points, following actual dependencies, targeted deep-dives on relevant code. Avoids reading unused code.
________________________________________
Q116: Your code generation session has been running for 30 minutes with multiple file modifications. The agent's recent generations are lower quality and miss context from earlier decisions. What's the issue?
A) The agent is tired B) Context window filling with tool results and conversation history, reducing generation capacity C) The model needs to be reset D) Too many files modified
Answer: B - Long sessions accumulate context (tool results, conversation). This reduces available space for generation, affecting quality. Context management (trimming, summarizing) needed.
________________________________________
Q117: Claude Code is working on a feature that requires changes to: API endpoint, service layer, database model, tests, and documentation. How should context be managed across these changes?
A) Work on all files simultaneously B) Sequential focused work: complete one layer (API), document decisions, move to next layer (service), reference previous decisions C) Use multiple Claude Code instances D) Increase context window
Answer: B - Sequential layered work with decision documentation maintains coherence. Each layer builds on documented decisions from previous layers.
________________________________________
Q118: Claude Code uses the Run_tests tool which returns verbose output (hundreds of lines showing each test). Only failed tests and summary are relevant. What's the optimization?
A) Use full output for completeness B) Parse test output in the tool implementation to return only: summary (X passed, Y failed) and failed test details C) Use grep to filter output D) Compress the output
Answer: B - Tool output trimming: return only actionable information (failures, summary). Successful test details consume tokens without providing value.
________________________________________
Q119: During code review, Claude Code should escalate when it encounters: patterns it doesn't recognize, potential security issues outside its expertise, or architectural decisions beyond code quality. What makes this effective?
A) Confidence scoring B) Explicit escalation criteria with examples: "Escalate when: unfamiliar framework patterns, potential security vulnerabilities (XSS, SQL injection, etc.), architectural changes affecting multiple systems" C) Always escalate complex issues D) Sentiment analysis
Answer: B - Explicit criteria with concrete examples establish clear escalation boundaries. Agent knows what it should/shouldn't handle.
________________________________________
Q120: Claude Code is generating comprehensive test suite (100+ tests). How should test generation be structured to maintain quality and context efficiency?
A) Generate all tests in one prompt B) Iterative generation: generate test categories (happy path, edge cases, errors), review each category, refine based on gaps, then implement C) Generate tests one at a time D) Use test templates
Answer: B - Iterative generation with review cycles: plan test categories, generate subset, identify gaps, refine, repeat. Maintains quality through validation loops.
________________________________________
________________________________________
SCENARIO 3: Multi-Agent Research System (60 Questions)
You are building a multi-agent research system using the Claude Agent SDK. A coordinator agent delegates to specialized subagents: one searches the web, one analyzes documents, one synthesizes findings, and one generates reports. The system researches topics and produces comprehensive, cited reports.
________________________________________
Domain 1: Agentic Architecture & Orchestration (16 questions)
Q121: In your multi-agent research system, when the coordinator delegates to the web_search subagent, does the subagent automatically see the coordinator's conversation history?
A) Yes, subagents inherit parent context B) No, subagents have isolated context; information must be explicitly passed C) Yes, but only the last 3 messages D) It depends on configuration
Answer: B - Critical concept: subagents have isolated context. Coordinator must explicitly pass necessary information in the Task tool call.
________________________________________
Q122: Your coordinator agent delegates a research subtask to the web_search subagent using the Task tool. What information should be included in the Task call?
A) Only the topic to research B) Task description, relevant context from previous research, specific questions to answer, quality criteria C) The coordinator's full conversation history D) Just the search query
Answer: B - Explicit context passing: task description, relevant prior findings, specific goals, success criteria. Subagent needs complete context to work effectively.
________________________________________
Q123: After the web_search subagent returns results, where does that information go?
A) Automatically to all other subagents B) To the coordinator agent's context C) To a shared memory all agents access D) To the synthesis agent directly
Answer: B - Hub-and-spoke: subagent results return to coordinator. Coordinator decides what to do next (delegate to another agent, synthesize, etc.).
________________________________________
Q124: Your research system covers "AI in creative industries" but only produces findings about visual arts, missing music, writing, film, and gaming. The web_search and document_analysis agents work correctly. What's the architectural issue?
A) Web_search agent needs better queries B) Document_analysis agent filters too aggressively C) Coordinator's task decomposition is too narrow - it only identified visual arts subtasks D) Synthesis agent needs gap detection tools
Answer: C - Task decomposition is the coordinator's responsibility. If it only creates visual arts subtasks, downstream agents execute correctly but with incomplete scope.
________________________________________
Q125: Your coordinator creates three subtasks: "research AI in art," "research AI in design," "research AI in photography." All three are assigned to web_search sequentially. What's the efficiency issue?
A) Too many subtasks B) Subtasks should be executed in parallel (spawn multiple Task tool calls in one coordinator turn) rather than sequentially C) Need more subagents D) Tasks are too similar
Answer: B - Parallel subagent execution: coordinator emits multiple Task calls in one response, they execute concurrently, results return together. Much faster than sequential.
________________________________________
Q126: After your web_search subagent completes research on "AI in music," the coordinator delegates to synthesis without checking if music coverage is comprehensive. The synthesis agent produces output noting major gaps in music coverage. What's the architectural fix?
A) Give synthesis agent web search tools B) Add an iterative refinement loop: coordinator evaluates synthesis, identifies gaps, re-delegates targeted research to web_search, repeats until coverage is sufficient C) Use a better synthesis prompt D) Increase web_search scope
Answer: B - Iterative refinement: evaluate output, identify gaps, targeted re-research, re-synthesize. This is a core multi-agent coordination pattern.
________________________________________
Q127: Your coordinator agent configuration includes this tool:
json
{
  "name": "Task",
  "description": "Delegate subtasks to specialized agents"
}
Your coordinator tries to use Task but fails. What's missing?
A) Tool implementation B) The coordinator's allowedTools must include "Task" for it to invoke subagents C) Subagent definitions D) Error handling
Answer: B - Coordinators must have "Task" in their allowedTools to spawn subagents. This is the mechanism for subagent invocation.
________________________________________
Q128: You're implementing error propagation. The web_search subagent times out and returns structured error information. What should this error include?
A) Just the error message B) Error category, attempted query, partial results collected before timeout, alternative approaches to try C) Stack trace D) Retry count
Answer: B - Structured error context enables intelligent coordinator recovery: what failed, what was partially accomplished, what alternatives exist.
________________________________________
Q129: Your coordinator delegates to web_search, which succeeds. Then it delegates to document_analysis, which fails due to timeout. The coordinator should:
A) Re-try document_analysis with the same input B) Evaluate the structured error: if transient with retry recommendation, retry; if unable to access documents, proceed with web_search results only and annotate missing document analysis C) Escalate immediately D) Restart the entire research
Answer: B - Intelligent error handling based on error category and context. Transient = retry; insurmountable = proceed with partial results + gap annotation.
________________________________________
Q130: In your research system, the synthesis subagent frequently needs to verify simple facts (dates, statistics) from findings. Currently it returns to coordinator, which delegates to web_search (2-3 round trips per verification). What's the optimization?
A) Give synthesis full web_search capabilities B) Give synthesis a scoped verify_fact tool that only works with already-collected sources, with instructions to route complex research to coordinator C) Cache verification results D) Synthesis should just output findings without verification
Answer: B - Scoped cross-role tool: limited capability for high-frequency simple needs, maintaining architectural boundaries. Complex work still goes through coordinator.
________________________________________
Q131: Your coordinator delegates tasks but doesn't track which subagent handled which part of the research. During synthesis, source attribution is lost. What should the coordinator do?
A) Store subagent results with metadata (which agent, what task, timestamp) B) Don't worry about attribution C) Have subagents tag their own results D) Use a database to track provenance
Answer: A - Coordinator maintains provenance: which agent produced which findings, what task they were executing. Essential for synthesis attribution.
________________________________________
Q132: Your research system has 5 specialized subagents. You notice the coordinator sometimes delegates to the wrong agent (e.g., asking document_analysis to search the web). What's the likely issue?
A) Too many subagents B) Subagent descriptions aren't clearly differentiated - each should explicitly state what it does, when to use it, and what it DOESN'T do C) Coordinator needs better instructions D) Use forced delegation
Answer: B - Like tool descriptions, subagent descriptions need clear differentiation: capabilities, use cases, boundaries, and explicit distinction from other agents.
________________________________________
Q133: Your coordinator creates one research subtask, delegates to web_search, waits for results, creates next subtask based on results, delegates to document_analysis, waits, etc. This is slow. What's the optimization?
A) Use faster subagents B) Plan all subtasks upfront, then execute in parallel where possible C) Use fewer subagents D) Increase timeout limits
Answer: B - Task planning + parallel execution: identify all independent subtasks upfront, execute them in parallel. Sequential execution should only be used when tasks have dependencies.
________________________________________
Q134: After your web_search subagent returns 10 sources with findings, the coordinator delegates to document_analysis with all 10 sources in context. The subagent processes them but context fills quickly. What's the optimization?
A) Use larger context window B) Have web_search return only essential fields (source URL, title, key excerpt) not full content; document_analysis retrieves full content only for sources it will analyze C) Summarize sources D) Process fewer sources
Answer: B - Context optimization through staged information: minimal metadata flows between agents, full content retrieved only when needed for actual analysis.
________________________________________
Q135: Your coordinator delegates simultaneously to web_search and document_analysis (both researching the same topic independently). How should their results be integrated?
A) First-come-first-served B) Coordinator receives both results, analyzes for overlap and conflicts, deduplicates findings, merges complementary information, delegates to synthesis with unified findings C) Each goes to synthesis separately D) Discard one set of results
Answer: B - Coordinator-managed integration: deduplicate, identify conflicts, merge complementary findings. Coordinator orchestrates information flow.
________________________________________
Q136: Your multi-agent system has this architecture: user → coordinator → subagents → coordinator → user. A subagent wants to delegate to another subagent. Should this be allowed?
A) Yes, for efficiency B) No, maintain hub-and-spoke: subagents communicate only through coordinator to preserve observability and control C) Yes, if tasks are related D) Only for error handling
Answer: B - Hub-and-spoke architecture: all inter-subagent communication through coordinator. Provides centralized orchestration, error handling, and information flow management.
________________________________________
Domain 2: Tool Design & MCP Integration (11 questions)
Q137: Your web_search subagent has these tools: google_search, bing_search, academic_search, news_search. The agent struggles to choose the right one. What's the fix?
A) Combine into one search tool with a source parameter B) Expand each tool's description: "google_search: General web search, best for current information, news, blogs. Use for: contemporary topics... | academic_search: Scholarly articles and papers, peer-reviewed sources. Use for: research studies, academic citations..." C) Use tool_choice to force selection D) Reduce to two tools
Answer: B - Clear differentiation: what each tool searches, when to use it, what types of sources it returns. Enables reliable selection.
________________________________________
Q138: Your document_analysis subagent can encounter various errors: PDF parsing failure, timeout, file not found, unsupported format. How should errors be structured?
A) Return error strings B) Structured: {"isError": true, "errorCategory": "transient|validation|format_unsupported", "isRetryable": bool, "attempted_document": "...", "partialResults": {...}, "alternativeApproaches": [...]} C) Throw exceptions D) Return empty results
Answer: B - Structured errors enable coordinator recovery: transient (retry), validation (check input), unsupported format (try different approach or skip).
________________________________________
Q139: Your research system has 8 tools across all agents: web_search (3 tools), document_analysis (2 tools), synthesis (1 tool), coordinator (2 tools). Should all 8 be available to every agent?
A) Yes, for maximum flexibility B) No, distribute tools by role: web_search agent gets only search tools, document_analysis gets only analysis tools, etc. C) Yes, but with guidance on when to use each D) Give core tools to everyone
Answer: B - Tool distribution by agent role maintains separation of concerns and improves selection reliability. Agents only see tools relevant to their function.
________________________________________
Q140: Your web_search tool returns 10-15 sources per query with full article text (thousands of tokens). Downstream agents need only source metadata and key excerpts. What's the optimization?
A) Keep full text for completeness B) Return structured metadata: {source_url, title, author, publication_date, relevance_score, key_excerpt (200 chars)} C) Compress full text D) Return links only
Answer: B - Trim tool outputs: return actionable information (metadata, key excerpts) not full content. Full content can be retrieved later if needed.
________________________________________
Q141: You're configuring MCP servers for your research system. You have servers for: academic databases, news APIs, and document storage. Should these be project-level or user-level configuration?
A) User-level since research is personal B) Project-level so all team members have consistent access to research sources C) Some project, some user D) System-level
Answer: B - Team-shared research tools belong in project-level MCP configuration. Ensures consistent source access across team.
________________________________________
Q142: Your synthesis agent needs to combine findings from multiple agents. It receives structured data:
json
{
  "findings": [...],
  "agent": "web_search",
  "task": "AI in music",
  "timestamp": "..."
}
Is this metadata useful?
A) No, only findings matter B) Yes, provenance tracking (which agent, what task) enables proper attribution and source citation in the final report C) Only for debugging D) It wastes tokens
Answer: B - Metadata enables provenance: where information came from, what question it answers, when it was collected. Essential for citation and attribution.
________________________________________
Q143: Your research tools can fail: web_search might be rate-limited, document_analysis might hit paywalls, academic_search might require institutional access. How should unavailable tools be communicated?
A) Return empty results B) Structured error: {"isError": true, "errorCategory": "permission", "isRetryable": false, "reason": "institutional_access_required", "alternative_tools": ["google_scholar"]} C) Fail silently D) Retry indefinitely
Answer: B - Permission errors with alternatives enable coordinator to route around limitations. Rate limits (transient) vs access denied (permission) require different handling.
________________________________________
Q144: Your coordinator has access to Task tool for delegation. Should subagents also have Task tools?
A) Yes, for recursive delegation B) No, subagents shouldn't spawn other subagents - maintain hub-and-spoke architecture where only coordinator delegates C) Only for the synthesis agent D) Yes, for efficiency
Answer: B - Hub-and-spoke: only coordinator delegates. Subagents don't spawn other subagents. This maintains orchestration visibility and control.
________________________________________
Q145: Your web_search tool description is: "Searches the web." Your academic_search tool is: "Searches academic sources." The coordinator frequently uses web_search when academic sources are needed. What's missing?
A) The coordinator needs better instructions B) Tool descriptions need clear differentiation: "web_search: General web (blogs, news, commercial). Returns: recent but not peer-reviewed. Use for: current events, opinions. academic_search: Peer-reviewed papers, journals. Returns: scholarly but may be older. Use for: research studies, citations." C) Combine the tools D) Use different tool names
Answer: B - Explicit differentiation: source types, content characteristics, when to use each. Boundary clarity improves selection.
________________________________________
Q146: Your document_analysis tool processes PDFs and returns extracted text. Sometimes text extraction is perfect, sometimes poor (scanned documents with OCR errors). Should the tool indicate extraction quality?
A) No, just return the text B) Yes, return structured output: {"text": "...", "extraction_quality": "high|medium|low", "method": "native_text|ocr", "confidence": 0.95} C) Only return high-quality extractions D) Pre-process to ensure quality
Answer: B - Quality metadata enables downstream decisions: high-quality text can be analyzed directly, low-quality might need manual review or different processing.
________________________________________
Q147: You're designing tools for your coordinator agent. It needs to: delegate to subagents, evaluate coverage, identify gaps. Should these be tools or built into the system prompt?
A) Tools: Task (delegation), evaluate_coverage, identify_gaps - makes capabilities explicit and observable B) System prompt only C) Just Task tool, rest in prompt D) No tools needed
Answer: A - Explicit tool-based capabilities make the coordinator's decision process observable and structured. Tools force structured thinking about delegation, evaluation, and gap analysis.
________________________________________
Domain 3: Claude Code Configuration & Workflows (12 questions)
Q148: Your research team uses Claude Code to draft research reports. You want to provide citation formatting requirements, research quality standards, and report structure guidelines. Where should this be configured?
A) In each report as a prompt B) In .claude/CLAUDE.md in your research repository C) In a Word template D) In each researcher's personal notes
Answer: B - Project-level CLAUDE.md provides shared context (standards, requirements, guidelines) for all team members working in the repository.
________________________________________
Q149: Individual researchers have personal preferences for report writing style (some prefer academic tone, others journalistic style). Where should personal preferences be configured?
A) Project CLAUDE.md B) User-level ~/.claude/CLAUDE.md C) They should follow team standards only D) In report headers
Answer: B - User-level configuration for personal preferences that don't affect team standards.
________________________________________
Q150: Your research repository has subdirectories for different research domains: medical/, technology/, social-sciences/. Each domain has specific citation standards and terminology. How should this be configured?
A) Put all standards in root CLAUDE.md B) Create .claude/rules/medical.md, technology.md, etc. with path patterns: paths: ["medical/**/*"] C) Create CLAUDE.md in each subdirectory D) Researchers manually specify domain
Answer: B - Path-specific rules automatically apply domain-specific standards based on file location. Ideal for directory-based organization.
________________________________________
Q151: Your team's citation formatting guide is used across 10 different research projects. How should this be configured for maintainability?
A) Copy into each project's CLAUDE.md B) Create standards/citations.md and use @import standards/citations.md in each project C) Keep in shared drive D) Wiki reference
Answer: B - @import enables reusable standards. Single source of truth for citations, imported in each project.
________________________________________
Q152: You create a custom command /cite that formats citations in your team's preferred style. Where should this be located for team availability?
A) .claude/commands/cite.sh in the repository B) ~/.claude/commands/cite.sh in each user's home directory C) System path D) As a skill
Answer: A - Project-scoped commands in version control are automatically available to all team members.
________________________________________
Q153: Claude Code is drafting a research report that requires analyzing 50 source documents. Full analysis would generate very verbose output. How should this be structured?
A) Analyze all documents in main conversation B) Create a skill with context: fork for verbose analysis, returning only structured findings to main conversation C) Reduce analysis depth D) Analyze fewer documents
Answer: B - Fork context isolates verbose work. Skill performs detailed analysis in isolation, returns structured summary to main context.
________________________________________
Q154: Your research team's CLAUDE.md includes: citation formats, research quality criteria, report structures, writing style guidelines, fact-checking procedures, peer review criteria. It's 2000 lines. Researchers report inconsistent Claude behavior. What's the issue?
A) CLAUDE.md is too detailed B) Large, unfocused context reduces effectiveness; split into modules (citations.md, quality_standards.md, etc.) and use @import C) Format issues D) Claude Code limits
Answer: B - Overly long context dilutes effectiveness. Modular organization with imports provides focused, relevant context.
________________________________________
Q155: Your research repository CLAUDE.md has academic writing guidelines. A researcher has personal preference for more casual style in draft notes (not final reports). Where should they configure this?
A) Override project CLAUDE.md B) User-level ~/.claude/CLAUDE.md for personal drafting style, but final reports should follow project standards C) Comment out project guidelines D) Create separate repository
Answer: B - User-level for personal work; project standards apply to shared/final work. Researchers can have personal preferences for drafts.
________________________________________
Q156: You want to provide example research reports that demonstrate ideal structure, citation usage, and analysis depth. Where should these examples be?
A) In CLAUDE.md (3000 lines of examples) B) Separate example files in docs/examples/, referenced in CLAUDE.md C) External wiki D) Not needed
Answer: B - Extensive examples in separate files keep CLAUDE.md focused. Reference them for discoverability without cluttering core guidance.
________________________________________
Q157: Your research system uses Claude Code to organize findings. Does Claude Code support user-level configuration that applies across all research projects?
A) No, all configuration is project-specific B) Yes, via ~/.claude/CLAUDE.md and ~/.claude.json C) Only for commands D) Only for MCP servers
Answer: B - User-level configuration in home directory applies globally across all of that user's Claude Code work.
________________________________________
Q158: Your team's CLAUDE.md is committed to a public GitHub repository but includes internal research methodologies and proprietary source access. Is this appropriate?
A) Yes, more context helps B) No, CLAUDE.md in version control may be public; sensitive information should be in secure storage or accessed via tools with credentials C) Yes, if repository is private D) Only summaries should be included
Answer: B - Security consideration: version-controlled config may be exposed. Sensitive methodologies and credentials should be externalized.
________________________________________
Q159: You configure MCP servers for academic databases (requires institutional credentials). Configuration is in project .mcp.json with env var ${ACADEMIC_API_KEY}. A new team member reports they can't access academic sources. What should you verify?
A) They restarted Claude Code B) They have ACADEMIC_API_KEY environment variable set with their institutional credentials C) The .mcp.json was committed D) Their Claude Code version
Answer: B - Environment variable expansion requires users to set variables in their environment. Config references it; users provide values.
________________________________________
Domain 4: Prompt Engineering & Structured Output (12 questions)
Q160: Your research system generates reports with inconsistent citation formatting - sometimes footnotes, sometimes inline, sometimes end notes. What's the most reliable solution?
A) Detailed citation instructions B) Define a generate_report tool with JSON schema specifying report structure including citations: [{citation_text, citation_style: "footnote|inline|endnote", source_id}] C) Few-shot examples D) Post-process formatting
Answer: B - JSON schema enforces structured output format. Citations as structured data ensures consistency and enables automated formatting.
________________________________________
Q161: You want to extract key findings from research sources with consistent structure: claim, evidence, source, confidence. How do you ensure consistency?
A) Detailed extraction instructions B) Define extract_findings tool with JSON schema: [{"claim": "string", "evidence": "string", "source_url": "string", "confidence": "high|medium|low"}] C) Few-shot examples only D) Natural language extraction
Answer: B - JSON schema via tool_use guarantees extraction structure. Enables aggregation and analysis of findings.
________________________________________
Q162: Your research quality assessment rates sources (credibility 1-5, recency 1-5, relevance 1-5) but ratings are inconsistent. What improves consistency?
A) Average multiple ratings B) Few-shot examples with explicit criteria for each dimension: "Credibility 5: peer-reviewed, established journal [example]. Credibility 3: blog by expert [example]. Recency 5: <6 months [example]." C) Simplify rating D) Use metadata only
Answer: B - Explicit criteria with concrete examples establish consistent rating standards across dimensions.
________________________________________
Q163: Your synthesis agent combines findings from multiple sources. You want output format: introduction, findings by theme, supporting evidence with citations, conclusion. What ensures consistency?
A) Detailed formatting instructions B) 2-3 few-shot examples showing complete report structure with proper theme organization and citation integration C) Template file D) Post-processing
Answer: B - Few-shot examples demonstrate complex output structure (organization, formatting, citation integration) more effectively than instructions.
________________________________________
Q164: Your web_search subagent finds 20 sources but only 5 are relevant. Document_analysis processes all 20, wasting resources. How do you optimize source selection?
A) Better search queries B) Add a relevance_assessment step: extract source metadata, rate relevance (1-5), pass only high-relevance sources to document_analysis C) Process all to be thorough D) Random sampling
Answer: B - Filtering stage: quick relevance assessment before expensive processing. Structured assessment enables threshold-based selection.
________________________________________
Q165: Your research system processes overnight batches of 100 source documents for analysis. Processing doesn't need immediate results. What's the optimal approach?
A) Synchronous Messages API B) Message Batches API for 50% cost savings C) Parallel API clients D) Streaming
Answer: B - Message Batches API for non-blocking, latency-tolerant batch processing. Significant cost savings for overnight work.
________________________________________
Q166: You submit 100 documents to Message Batches API. After processing, 8 failed: 5 timeouts, 3 format errors. What's the retry strategy?
A) Retry all 8 identically B) Retry 5 timeouts (transient); analyze 3 format errors to determine if documents need preprocessing before retry C) Discard failures D) Resubmit entire batch
Answer: B - Error-aware retry: transient errors warrant immediate retry; format errors may need preprocessing or may be unprocessable.
________________________________________
Q167: Your coordinator's task decomposition is inconsistent - sometimes creating comprehensive subtasks, sometimes missing entire sub-domains. What improves consistency?
A) Better coordinator instructions B) Few-shot examples showing thorough task decomposition for different topics: "Topic: X. Subtasks: [comprehensive list covering all sub-domains]" C) Always use the same decomposition D) More subagents
Answer: B - Few-shot examples demonstrate thorough decomposition patterns, showing how to systematically cover all aspects of a topic.
________________________________________
Q168: Your synthesis agent outputs findings but sometimes fabricates details not present in sources. How do you prevent this?
A) Lower temperature B) Require synthesis output to include source references for each claim, enabling verification: [{"claim": "...", "supporting_sources": [source_ids]}] C) More sources D) Confidence scoring
Answer: B - Structured output with source attribution enables verification and discourages fabrication. Each claim must be tied to sources.
________________________________________
Q169: Your report generation produces varied quality - some reports are thorough with strong analysis, others are superficial summaries. What improves consistency?
A) More detailed instructions B) Few-shot examples showing ideal reports with proper depth: comprehensive introduction, multi-faceted analysis, evidence integration, nuanced conclusions C) Longer max_tokens D) Multiple drafts
Answer: B - Few-shot examples demonstrate quality standards (depth, analysis, integration, nuance) more effectively than descriptive criteria.
________________________________________
Q170: Your web_search subagent returns findings, but synthesis complains about insufficient coverage. You implement an iterative refinement loop. How does this work?
A) Re-search automatically B) Coordinator evaluates synthesis output, identifies specific gaps, creates targeted research tasks for those gaps, re-synthesizes with new findings C) Use more sources initially D) Longer search queries
Answer: B - Iterative refinement: evaluate → identify gaps → targeted re-research → re-synthesize. Specific gap identification enables efficient refinement.
________________________________________
Q171: Your document_analysis extracts claims from academic papers but sometimes misinterprets statistical significance or study limitations. How do you improve extraction accuracy?
A) More detailed extraction instructions B) Few-shot examples showing correct interpretation of statistical results and study limitations with explanations of common misinterpretation pitfalls C) Extract only direct quotes D) Higher confidence threshold
Answer: B - Few-shot examples with explanations educate the model about domain-specific interpretation (statistics, methodology, limitations).
________________________________________
Domain 5: Context Management & Reliability (9 questions)
Q172: Your research coordinator delegates to 5 subagents sequentially. By the 4th delegation, you notice it refers to "earlier findings" generically rather than specifically. What's the issue?
A) Too many delegations B) Lost-in-the-middle: specific findings from earlier subagents (positions 1-2) lose detail by position 4-5 C) Context window full D) Coordinator needs better memory
Answer: B - Long sequences cause detail loss from middle positions. Earlier subagent results become vague by the time later agents are processed.
________________________________________
Q173: What pattern preserves critical research findings (key claims, important statistics, source attributions) across multiple subagent delegations?
A) Repeat all findings in each delegation B) Extract key findings into a structured "research summary" maintained at the top of coordinator context, updated after each subagent completes C) Use larger context window D) Process fewer findings
Answer: B - Structured summary extraction: key findings in persistent, prominently-positioned summary block. Resistant to position effects and summarization loss.
________________________________________
Q174: Your web_search tool returns sources with full article text (2000+ tokens per source, 10 sources = 20k tokens). Coordinator only needs source metadata for routing decisions. What's the optimization?
A) Use full text for completeness B) Web_search returns structured metadata: {source_id, url, title, author, date, relevance_score, 200-char excerpt}. Full text retrieved later if needed. C) Compress articles D) Fewer sources
Answer: B - Staged information flow: minimal metadata for routing decisions, full content retrieved only when needed for analysis. Optimizes context usage.
________________________________________
Q175: Your synthesis agent combines findings from multiple subagents. If source attribution is lost, citations can't be generated. How should subagent outputs be structured?
A) Plain text findings B) Structured findings with provenance: [{"finding": "...", "source_url": "...", "source_title": "...", "retrieved_by": "web_search", "relevance": 0.9}] C) Just the finding text D) Links only
Answer: B - Structured output with provenance metadata preserves attribution through synthesis. Enables accurate citation generation.
________________________________________
Q176: Your research session involves: topic decomposition → parallel web search → document analysis → initial synthesis → gap identification → targeted re-research → final synthesis. By final synthesis, context is 80% full. What's the issue?
A) Too many steps B) Each step accumulates tool results and intermediate outputs; context fills without trimming/summarizing between stages C) Need larger context window D) Process is too complex
Answer: B - Multi-stage processes accumulate context. Requires active management: summarize after each major stage, trim verbose intermediate results.
________________________________________
Q177: Which scenario most appropriately triggers escalation to human researcher?
A) Any conflict between sources B) Policy-relevant findings where implications are ambiguous or require expert judgment C) More than 10 sources found D) Low confidence scores
Answer: B - Escalate when human judgment is needed: ambiguous implications, policy decisions, expert interpretation. Conflicts can often be presented with attribution.
________________________________________
Q178: Your web_search subagent times out while researching a complex topic. It returns structured error with partial results from 3 sources and suggested alternative queries. What should the coordinator do?
A) Escalate immediately B) Evaluate error context: if transient, retry with suggested alternative (narrower query); use partial results and annotate incomplete coverage C) Discard partial results D) Try different subagent
Answer: B - Intelligent error handling: use error context (transient, partial results, alternatives) to make informed recovery decision.
________________________________________
Q179: Your research system processes 50 sources per topic. How should source analysis be batched to maintain quality and context efficiency?
A) Analyze all 50 simultaneously B) Batch processing: analyze in groups of 10, extract key findings from each batch, summarize batch findings, continue with next batch C) One source at a time D) Random sampling
Answer: B - Batch processing with progressive summarization: process subset, extract findings, summarize before next batch. Maintains detail while managing context.
________________________________________
Q180: Your synthesis agent should escalate when it identifies contradictory claims from credible sources that require expert resolution. How is this implemented?
A) Automatic escalation on any contradiction B) Structured output identifies contradictions: {"contradictions": [{"topic": "...", "claim_a": {"claim": "...", "sources": [...]}, "claim_b": {"claim": "...", "sources": [...]}}]}, enabling informed escalation decisions C) Ignore contradictions D) Always choose most recent source
Answer: B - Structured contradiction identification with full context (claims, sources) enables intelligent escalation decisions. Not all contradictions need escalation.
________________________________________
________________________________________
SCENARIO 4: Developer Productivity with Claude (60 Questions)
You are building developer productivity tools using the Claude Agent SDK. The agent helps engineers explore unfamiliar codebases, understand legacy systems, generate boilerplate code, and automate repetitive tasks. It uses the built-in tools (Read, Write, Bash, Grep, Glob) and integrates with Model Context Protocol (MCP) servers.
________________________________________
Domain 1: Agentic Architecture & Orchestration (16 questions)
Q181: A developer asks your agent to "understand how the authentication system works." What's the efficient exploration strategy?
A) Read all files in auth/ directory B) Use Grep to find authentication entry points (login routes, middleware), then Read key files, follow imports incrementally C) Use Bash to run auth tests D) Read random files
Answer: B - Incremental exploration from entry points. Grep finds relevant starting points, Read provides detail, following dependencies avoids reading irrelevant code.
________________________________________
Q182: Your agent is analyzing a large codebase (300+ files). Full analysis would exceed context limits. How should the exploration be structured?
A) Read all files upfront B) Use subagent delegation: coordinator plans exploration, specialized subagents perform focused analysis (find entry points, trace dependencies, analyze specific modules), coordinator synthesizes findings C) Use a 200k context model D) Process in batches of 100 files
Answer: B - Multi-agent architecture for large tasks: coordinator orchestrates, subagents perform focused work in isolated contexts, coordinator synthesizes.
________________________________________
Q183: Your developer productivity agent generates boilerplate code for new API endpoints. The pattern is: route handler → validation logic → service layer call → error handling → logging. How should generation be structured?
A) Generate all code in one response B) Sequential generation with validation: generate route handler, verify structure, generate validation, verify, etc. - allows early error detection C) Template-based generation D) Generate tests first
Answer: B - Sequential generation with validation steps enables early error detection and ensures each layer follows patterns before building the next.
________________________________________
Q184: A developer asks to "add comprehensive error handling to the API layer" (25 endpoint files). What should the agent do?
A) Direct execution: modify all files B) Plan mode: analyze current error handling patterns, identify gaps, propose consistent error handling strategy, get approval, then implement C) Generate error handling examples D) Modify one file as example
Answer: B - Plan mode for architectural changes across many files. Design validation before implementation prevents inconsistent patterns.
________________________________________
Q185: Your agent helps developers with code review. It analyzes PR diff and provides feedback. The agent's agentic loop should use which tools?
A) Only Read (to read changed files) B) Read (review files), Bash (run tests/linter), Grep (search for patterns), potentially Write (if suggesting fixes) C) Just Grep to find issues D) Bash only to run analysis tools
Answer: B - Code review requires multiple tools: Read for understanding, Bash for running checks, Grep for pattern detection, Write for generating fixes.
________________________________________
Q186: Your agent is refactoring a module (15 files). After several modifications, the developer wants to undo everything and try a different approach. How should sessions be managed?
A) No session management needed B) Use fork_session before refactoring to create a branch point; can return to fork origin to try alternative approaches C) Manual file restoration D) Version control rollback
Answer: B - fork_session enables exploring divergent approaches from a shared baseline. Like branching in git for exploratory work.
________________________________________
Q187: Your agent generates database migration scripts. Errors could be catastrophic. What's the appropriate safety pattern?
A) Generate and apply immediately B) Plan mode: analyze schema changes, identify risks, generate migration, show preview with rollback plan, require explicit approval before execution C) Always review migrations manually D) Test in production
Answer: B - Critical operations require plan mode: design review, risk analysis, preview, explicit approval. Safety gate before execution.
________________________________________
Q188: A developer asks to "optimize the dashboard loading time." This is ambiguous (database queries? frontend rendering? caching? API calls?). What should the agent do?
A) Start with database query optimization (most common) B) Plan mode: profile performance, identify bottlenecks, propose targeted optimizations with expected impact C) Ask developer to specify D) Optimize everything
Answer: B - Ambiguous tasks require investigation (plan mode): identify actual problems before proposing solutions. Avoids optimizing the wrong things.
________________________________________
Q189: Your agent is tracing a bug through multiple files. It reads File A, identifies a suspicious function, reads File B where it's called from, then File C which calls File B. Context is filling with file contents. What's the optimization?
A) Use larger context window B) Incremental exploration with summarization: read File A, extract relevant findings (the suspicious function signature and behavior), reference in summary, then read File B. Keep summary, not full file contents. C) Read fewer files D) Use Grep only
Answer: B - Progressive summarization: extract key findings from each file into a growing summary, don't accumulate full file contents. Maintains investigation thread without context bloat.
________________________________________
Q190: Your agent helps developers understand a complex state machine with 8 states and 20 transitions. Reading the implementation file (500 lines) would consume significant context. What's the efficient approach?
A) Read entire file B) Use Grep to extract state definitions and transition logic, then Read only complex transition handlers if needed C) Generate documentation instead D) Ask developer to explain
Answer: B - Targeted extraction: Grep for structure (states, transitions), understand architecture, Read details only for complex parts. Avoids full file read.
________________________________________
Q191: A developer wants to rename a variable used across 30 files. The agent should:
A) Use Write on all 30 files B) Use Grep to find all usages, verify scope, use Edit for precise renames (or Read + modify + Write if Edit fails due to non-unique context) C) Find-and-replace with Bash D) Manual renaming
Answer: B - Systematic approach: Grep to find usages (verify safety), Edit for targeted changes. Fallback to Read+Write if needed.
________________________________________
Q192: Your developer agent encounters a code pattern it doesn't recognize (unfamiliar framework, custom abstractions). What should it do?
A) Guess the pattern B) Present findings to developer: "Found unfamiliar pattern [example], appears to be [hypothesis]. Suggest: [options]. Need guidance on handling." C) Skip the code D) Generate generic code
Answer: B - Escalate with context when encountering unknowns. Provide what was found, initial hypothesis, ask for guidance. Effective human-AI collaboration.
________________________________________
Q193: Your agent generates code but occasionally includes placeholder comments like "// TODO: implement error handling" instead of actual implementation. What's the likely cause?
A) Agent is lazy B) Insufficient context or requirements for complete implementation; either provide more detail or accept placeholder for developer completion C) Temperature too low D) Token limit
Answer: B - Placeholders indicate missing information for complete implementation. Either provide more context or accept placeholders as appropriate delegation to developer.
________________________________________
Q194: A developer asks your agent to "document all public APIs in the codebase." There are 50+ API endpoints. What's the efficient approach?
A) Document all at once B) Batched approach: Glob to find API files, process in batches of 10, generate documentation for each batch, continue to next batch C) One at a time D) Generate index only
Answer: B - Batch processing for large tasks: manageable chunks, progressive documentation generation, context management between batches.
________________________________________
Q195: Your agent's tool execution fails (e.g., Grep returns no results). The agent should:
A) Retry the same command B) Reason about the failure: validate assumptions (file paths correct? pattern syntax valid?), adjust approach, try alternative strategies C) Report error and stop D) Try random alternatives
Answer: B - Intelligent failure handling: reason about why the tool failed, validate assumptions, adjust strategy. Part of the agentic loop's problem-solving.
________________________________________
Q196: A developer asks to "refactor the payment processing module to use the new payment gateway API." This affects 8 files and requires API migration. What's the approach?
A) Direct execution with detailed instructions B) Plan mode: understand current implementation, review new API requirements, design migration strategy, identify risk areas, propose implementation plan C) Refactor one file at a time without planning D) Generate examples only
Answer: B - Complex refactoring with external dependencies requires plan mode: understand both systems, design migration, risk assessment, phased implementation.
________________________________________
Domain 2: Tool Design & MCP Integration (11 questions)
Q197: You're creating MCP tools for your development environment. You have: run_tests, check_linting, build_project, deploy_staging, rollback_deployment. Should all developers have access to all tools?
A) Yes, for flexibility B) No, distribute by role: developers get tests, linting, build; only DevOps/leads get deploy, rollback C) Everyone gets everything with warnings D) Use permissions at runtime
Answer: B - Tool distribution by role and trust level. Deployment tools should be restricted to appropriate roles.
________________________________________
Q198: Your MCP tool analyze_codebase can fail for various reasons: invalid path, permission denied, out of memory, unsupported language. How should errors be structured?
A) Return error strings B) Structured: {"isError": true, "errorCategory": "validation|permission|resource|unsupported", "isRetryable": bool, "attempted_path": "...", "suggestion": "..."} C) HTTP status codes D) Exceptions
Answer: B - Structured errors enable intelligent handling: validation (fix input), permission (check access), resource (adjust scope), unsupported (skip or use alternative).
________________________________________
Q199: Your codebase MCP server provides: get_file, search_code, get_git_history, run_tests, profile_performance, check_security, deploy_to_prod. A code review agent uses this server. Which tools should it have access to?
A) All tools B) Only review-relevant tools: get_file, search_code, get_git_history, run_tests, check_security (NOT profile, NOT deploy) C) Just get_file D) Depends on the review
Answer: B - Focused tool sets by agent purpose. Code review doesn't need profiling or deployment tools.
________________________________________
Q200: Your MCP tool run_tests returns verbose output (all test names, timing, coverage details - hundreds of lines). Most uses only need: pass/fail status and failed test details. What's the optimization?
A) Keep verbose output for debugging B) Create two tools: run_tests_summary (status + failures only) and run_tests_detailed (full output) C) Always return full output D) Compress output
Answer: B - Different detail levels for different needs. Summary for quick checks, detailed for deep investigation.
________________________________________
Q201: You configure MCP servers for development tools (GitHub, Jira, Jenkins). Should this be project-level or user-level?
A) User-level since development environments vary B) Project-level so all team members have consistent tooling access C) Mixed D) System-level
Answer: B - Team-shared development tools belong in project-level MCP config. Consistent tooling across team.
________________________________________
Q202: Your MCP tool get_file_contents returns complete file contents. For large files (>1000 lines), this consumes significant context. Should there be a summary option?
A) No, complete contents are always needed B) Yes, add parameter summary: bool that returns file structure (imports, exports, function signatures) instead of full implementation C) Truncate large files D) Use compression
Answer: B - Summary mode for large files: structure and signatures often sufficient for navigation/understanding. Full contents retrieved when needed.
________________________________________
Q203: Your development MCP server uses team API keys for shared services (CI/CD, monitoring). Where should this be configured?
A) Hardcoded in .mcp.json B) Project .mcp.json with environment variable ${TEAM_CI_API_KEY}, team stores key in shared secure vault C) User-level with each developer's key D) In code
Answer: B - Shared services use team credentials via environment variables. Centralized secret management, consistent access.
________________________________________
Q204: Your MCP tool descriptions for refactor_code and optimize_code are similar, causing selection confusion. How should they be differentiated?
A) Rename the tools B) Expand descriptions: "refactor_code: Restructures code for better maintainability (extract methods, rename variables, simplify logic). Use for: code cleanup, readability. optimize_code: Improves runtime performance (algorithm efficiency, caching). Use for: slow operations, bottlenecks." C) Merge them D) Add examples to system prompt
Answer: B - Clear purpose differentiation: what each does (restructure vs performance), when to use (maintainability vs speed).
________________________________________
Q205: You have MCP tools for: lint_python, lint_javascript, lint_typescript, lint_go. The agent frequently chooses the wrong linter for the file type. What's the fix?
A) Consolidate into one lint tool with file type detection B) Each tool description includes: "lint_python: ONLY for .py files. Use for: Python code quality checks." C) Use file extension matching in tool selection D) Better agent instructions
Answer: A - Related operations on different inputs (lint various languages) belong in one tool with automatic language detection. Reduces selection complexity.
________________________________________
Q206: Your MCP tool deploy_to_staging should only be callable from tested, linted code. Where should this validation be?
A) In the tool implementation (check tests passed, linting passed before deploying) B) In tool description (tell agent to check first) C) In agent instructions D) Developer responsibility
Answer: A - Safety validations with compliance requirements belong in tool implementation. Programmatic enforcement.
________________________________________
Q207: Your codebase MCP server provides tools to multiple agents (review agent, refactoring agent, documentation agent). Should each agent see the same tool descriptions?
A) Yes, descriptions are universal B) Consider context: descriptions could emphasize different aspects for different agents, but core functionality stays the same C) Completely different descriptions per agent D) No descriptions needed
Answer: A - Tool descriptions should be universal and complete. Agents with different purposes will naturally select relevant tools. Over-customization adds complexity.
________________________________________
Domain 3: Claude Code Configuration & Workflows (12 questions)
Q208: Your development team uses Claude Code for code generation. You want to provide: coding standards, architecture patterns, testing requirements, security guidelines. Where should this be configured?
A) In each developer request B) In .claude/CLAUDE.md in the repository C) External documentation D) Code comments
Answer: B - Project-level CLAUDE.md provides persistent context (standards, patterns, requirements) for all team development work.
________________________________________
Q209: Your codebase has different conventions: React components (hooks, prop-types), API routes (Express patterns), database models (Sequelize), tests (Jest). Files are spread throughout the codebase. How do you ensure Claude applies the right conventions automatically?
A) Root CLAUDE.md with all conventions B) .claude/rules/ files with glob patterns: react.md with paths: ["**/*.jsx", "**/*.tsx"], api.md with paths: ["**/routes/**"], tests.md with paths: ["**/*.test.js"] C) Directory-level CLAUDE.md files D) Skills for each type
Answer: B - Path-specific rules with glob patterns automatically activate based on file type/location, regardless of directory structure.
________________________________________
Q210: Your team's JavaScript style guide is shared across 8 microservices. How should this be configured maintainably?
A) Copy into each repo's CLAUDE.md B) Create standards/javascript.md, use @import standards/javascript.md in each repo C) Wiki reference D) Each developer configures personally
Answer: B - @import for reusable standards. Single source of truth, imported in each project.
________________________________________
Q211: You create a custom command /review-pr that runs linting, tests, and generates review comments. Where should this be located for team availability?
A) .claude/commands/review-pr.sh in repository B) Each developer's ~/.claude/commands/ C) System path D) As MCP tool instead
Answer: A - Project-scoped commands in version control, automatically available to all team members.
________________________________________
Q212: You create a skill for analyzing code complexity:
yaml
---
context: fork
allowed-tools: [Read, Grep, Glob, Bash]
---
What benefit does context: fork provide?
A) Faster execution B) Isolates verbose analysis (reading many files, detailed metrics) from main conversation C) More tool access D) Parallel execution
Answer: B - Fork context isolates verbose exploratory work, preventing context pollution in main conversation.
________________________________________
Q213: Your repository has tests in multiple locations: tests/ directory, __tests__/ directories, *.test.js files co-located with code. You want testing conventions to apply to all tests. Configuration?
A) Multiple CLAUDE.md files B) .claude/rules/testing.md with paths: ["**/tests/**", "**/__tests__/**", "**/*.test.js", "**/*.spec.js"] C) Root CLAUDE.md D) Testing skill
Answer: B - Glob patterns match files regardless of location. Applies testing conventions to all test files across codebase.
________________________________________
Q214: Your team's CLAUDE.md includes architecture diagrams (as ASCII art), extensive code examples, and detailed pattern explanations. It's 2500 lines. Claude's code generation quality is inconsistent. What's the issue?
A) Too much information is harmful B) Large, unfocused context reduces effectiveness; split into focused modules (architecture.md, patterns.md, examples.md) with @import C) Format issues D) Claude Code limitations
Answer: B - Overly long context dilutes effectiveness. Modular organization with imports provides relevant context without overwhelming.
________________________________________
Q215: Individual developers have personal coding preferences (variable naming style, comment verbosity) that differ from team standards. Where should personal preferences be configured?
A) They must follow team standards B) User-level ~/.claude/CLAUDE.md for personal projects/exploration, but team standards apply to shared code C) Override project CLAUDE.md D) Not possible to configure
Answer: B - User-level for personal work; project standards for team code. Allows personal preference in appropriate contexts.
________________________________________
Q216: Your project CLAUDE.md includes internal API endpoints and database schema details. The repo is private now but may become open-source. Is this appropriate?
A) Yes, helpful context B) Consider sensitivity: internal details in version control may be exposed if repo goes public; consider externalizing or using tool access C) No, never include details D) Only summaries
Answer: B - Forward-looking security consideration: what's private today may be public tomorrow. Sensitive internals should be carefully considered.
________________________________________
Q217: You configure an MCP server in .mcp.json for team code analysis tools. A new team member reports tools aren't available. What should you check?
A) They installed the MCP server B) They restarted Claude Code, and the .mcp.json file was committed to the repository C) Their Claude Code version D) Their permissions
Answer: B - Project-level MCP config must be in version control. New members get it automatically when cloning. Restart Claude Code to discover it.
________________________________________
Q218: Your CLAUDE.md provides coding standards. You want to include example code showing good vs bad patterns. Should this be in CLAUDE.md?
A) Yes, directly in CLAUDE.md (keep it short) B) Extensive examples in separate files docs/examples/, referenced from CLAUDE.md C) No examples needed D) Comments in actual code
Answer: B - Extensive examples in separate files keep CLAUDE.md focused. Reference them for discoverability without bloating core guidance.
________________________________________
Q219: You want different coding conventions for production code vs test code vs build scripts. How should this be configured?
A) One CLAUDE.md with all conventions B) Path-specific rules: production.md with paths: ["src/**/*"], tests.md with paths: ["**/*.test.*"], build.md with paths: ["scripts/**", "*.config.js"] C) Skills for each type D) Not possible
Answer: B - Path-specific rules automatically apply appropriate conventions based on file type/location. Perfect for different standards by file purpose.
________________________________________
Domain 4: Prompt Engineering & Structured Output (12 questions)
Q220: Claude Code generates functions with inconsistent documentation - some have detailed docstrings, some have minimal comments, some have none. What ensures consistency?
A) Detailed documentation instructions B) 2-3 few-shot examples showing ideal documentation format for functions, classes, and modules C) Post-generation documentation pass D) Linting rules
Answer: B - Few-shot examples demonstrate documentation patterns (format, detail level, what to document) more effectively than instructions.
________________________________________
Q221: You want Claude to extract API endpoints from code with consistent structure: method, path, parameters, response format, error codes. How do you ensure consistency?
A) Detailed extraction instructions B) Define extract_api_endpoints tool with JSON schema: [{"method": "GET|POST|PUT|DELETE", "path": "string", "parameters": [...], "response_schema": {...}, "error_codes": [...]}] C) Use Grep patterns D) Few-shot examples only
Answer: B - JSON schema via tool_use guarantees structured extraction format. Enables automated API documentation generation.
________________________________________
Q222: Claude Code reviews code and rates quality (1-5), but ratings are inconsistent - similar code gets different scores. What improves consistency?
A) Simplify to pass/fail B) Few-shot examples with explicit criteria: "5: Well-structured, tested, documented, efficient [code example]. 3: Functional but needs improvements [code example]. 1: Critical issues [code example]" C) Average multiple ratings D) Use static analysis scores
Answer: B - Explicit criteria with concrete code examples establish consistent rating standards.
________________________________________
Q223: You want Claude Code to generate unit tests with consistent structure (arrange-act-assert) and naming (test_should_behavior_when_condition). What ensures consistency?
A) Testing framework enforces this B) 2-3 few-shot examples in CLAUDE.md showing ideal test structure, naming, and organization C) Detailed instructions D) Test templates
Answer: B - Few-shot examples demonstrate test patterns (structure, naming, organization, assertion style) more effectively than specifications.
________________________________________
Q224: Claude Code generates error handling but quality varies - sometimes comprehensive try-catch with specific error types, sometimes generic handling. What improves consistency?
A) Always use the same error handling pattern B) Few-shot examples showing error handling patterns for different scenarios: API calls (network errors), file operations (I/O errors), validation (data errors) C) Linting rules D) Error handling library
Answer: B - Few-shot examples demonstrate context-appropriate error handling across different scenarios.
________________________________________
Q225: Claude Code analyzes a 15-file PR. You notice superficial review of some files while others get detailed analysis. What's the architectural solution?
A) Use larger model B) Multi-pass review architecture: per-file pass (consistent depth on each file), then integration pass (cross-file issues) C) Review fewer files D) More detailed instructions
Answer: B - Multi-pass: focused per-file passes ensure consistent depth, separate integration pass catches cross-file issues. Addresses attention dilution.
________________________________________
Q226: You want Claude Code to identify code smells with structured output for tracking: smell type, location, severity, suggestion. What's the approach?
A) Natural language list of issues B) Define find_code_smells tool with JSON schema: [{"smell_type": "long_function|deep_nesting|duplication|magic_number|god_class", "file": "...", "line": number, "severity": "high|medium|low", "suggestion": "..."}] C) Use linting tools instead D) Grep for patterns
Answer: B - Structured output via JSON schema enables automated tracking, aggregation, metrics, and trend analysis.
________________________________________
Q227: Claude Code generates API endpoint boilerplate (route, validation, handler, error handling, logging). Consistency across endpoints is critical. What ensures consistency?
A) Detailed template specs B) 2-3 few-shot examples showing complete endpoint implementations following the established pattern C) Code generator tool D) Copy-paste from existing endpoint
Answer: B - Few-shot examples of complete patterns (showing how all layers integrate) ensure consistent boilerplate generation.
________________________________________
Q228: Claude Code refactors a module (8 files). You want to preserve refactoring decisions (naming conventions, pattern changes) throughout. What's the approach?
A) Repeat instructions for each file B) Maintain a structured "refactoring plan" at the start of context: decisions made, naming conventions, pattern changes, migration checklist C) Larger context window D) Process files individually
Answer: B - Structured plan/decision doc persists throughout refactoring session. Resistant to context degradation, provides consistent reference.
________________________________________
Q229: You're generating database migration scripts. They must be safe and correct. What's the approach?
A) Generate directly B) Plan mode: analyze current schema, identify changes, generate migration with rollback script, preview changes, explain risks, require approval C) Always manually write migrations D) Use migration tool
Answer: B - Critical operations require plan mode: design review, risk assessment, preview, approval before execution.
________________________________________
Q230: Claude Code generates code comments, but style varies wildly. You need consistent JSDoc format with specific sections. What's the approach?
A) Detailed JSDoc specification B) 2-3 few-shot examples showing JSDoc format for functions (with params, returns, examples), classes (with properties, methods), and modules (with overview, usage) C) Post-process with documentation generator D) Linting rules
Answer: B - Few-shot examples demonstrate documentation format across different contexts more effectively than format specifications.
________________________________________
Q231: Claude Code generates test cases but some tests are flaky (timing dependencies, shared state). You want the generator to avoid flaky patterns. What's the approach?
A) Instructions to avoid flakiness B) Few-shot examples contrasting flaky patterns (timing dependencies, shared state, external services) with robust patterns (mocking, isolated state, deterministic tests) C) Run tests multiple times D) Test review pass
Answer: B - Few-shot examples showing anti-patterns vs best practices educate the generator about what to avoid and why.
________________________________________
Domain 5: Context Management & Reliability (9 questions)
Q232: Claude Code is exploring a large codebase (200+ files). By the 50th file, references to earlier files become vague ("the earlier auth module" instead of specific file names). What's the issue?
A) Too many files B) Lost-in-the-middle effect: early files (positions 1-20) lose specificity by position 50 C) Context window full D) Agent memory limitation
Answer: B - Long sequences cause detail loss from earlier positions. Progressive summarization or position effects.
________________________________________
Q233: During large codebase exploration, what pattern preserves critical architectural findings (key modules, dependencies, design patterns) throughout the session?
A) Re-read files as needed B) Extract architectural findings into structured "codebase map" maintained at top of context: key modules, their responsibilities, dependencies, patterns C) Larger context window D) Process fewer files
Answer: B - Structured knowledge extraction: architectural map/summary persists prominently, resistant to degradation.
________________________________________
Q234: Claude Code's get_file_contents tool returns complete files. For a 500-line file where only one 20-line function is relevant, what's the optimization?
A) Always get complete file for context B) Use Grep to extract the specific function, or Read file with focused attention on the relevant section C) Truncate large files D) Summarize everything
Answer: B - Targeted extraction: Grep for specific content, or focused Read with clear purpose. Avoid loading irrelevant code.
________________________________________
Q235: Claude Code analyzes code quality across 30 files. Each file's analysis generates detailed output (metrics, issues, suggestions). Context fills with analysis results. What's the optimization?
A) Use 200k context model B) Batch analysis with progressive summarization: analyze 10 files, extract key findings summary, continue with next batch using summary instead of full details C) Reduce analysis depth D) Analyze fewer files
Answer: B - Batch processing with progressive summarization: full analysis → key findings extraction → continue. Maintains insight without detail accumulation.
________________________________________
Q236: Claude Code is refactoring a module over 20 file modifications. By modification 15, generation quality decreases (less thorough, missing context from earlier changes). What's the issue?
A) Agent is tired B) Long session accumulates context (tool results, modifications); reduces generation capacity C) Need model reset D) Too many changes
Answer: B - Long sessions accumulate context, reducing available generation space. Requires active context management.
________________________________________
Q237: Claude Code should escalate when it encounters: unfamiliar frameworks, potential security vulnerabilities beyond its expertise, or architectural decisions requiring human judgment. What makes escalation effective?
A) Confidence scoring B) Explicit escalation criteria with examples: "Escalate when: unfamiliar patterns [example], security concerns [XSS, SQL injection, etc.], architectural impacts [multi-system changes]" C) Always escalate complex code D) Sentiment analysis
Answer: B - Explicit criteria with concrete examples establish clear escalation boundaries.
________________________________________
Q238: During code generation, Claude Code creates functions calling APIs, but occasionally uses fictional API endpoints. How do you prevent hallucination?
A) Lower temperature B) Provide explicit API documentation/schema in CLAUDE.md or via tools; generate code that references documented APIs only C) More examples D) Confidence thresholds
Answer: B - Grounding: provide explicit, factual API documentation. Generated code must reference documented endpoints, reducing fabrication.
________________________________________
Q239: Claude Code generates 50 test cases for a module. How should generation be structured for quality and context efficiency?
A) Generate all 50 at once B) Iterative: generate test categories (happy path, edge cases, errors), review each category for gaps, generate implementations, verify coverage C) One test at a time D) Random generation
Answer: B - Iterative generation with validation: plan test categories, generate subset, review for gaps, refine. Maintains quality through validation loops.
________________________________________
Q240: Your developer agent encounters code in an unfamiliar programming language. What should it do?
A) Generate generic code B) Escalate with context: "Found [language] code [example]. Unfamiliar with [language] patterns. Options: 1) Attempt basic changes with high risk, 2) Recommend specialist review, 3) Suggest alternative approaches" C) Skip the file D) Use general programming knowledge
Answer: B - Transparent escalation with context: what was found, what's unknown, what options exist, ask for guidance. Effective collaboration on unknowns.
________________________________________
________________________________________
SCENARIO 5: Claude Code for Continuous Integration (60 Questions)
You are integrating Claude Code into your Continuous Integration/Continuous Deployment (CI/CD) pipeline. The system runs automated code reviews, generates test cases, and provides feedback on pull requests. You need to design prompts that provide actionable feedback and minimize false positives.
________________________________________
Domain 1: Agentic Architecture & Orchestration (16 questions)
Q241: Your CI pipeline runs Claude Code for PR review. The command hangs indefinitely waiting for interactive input. What flag is required?
A) --batch B) --ci-mode C) -p or --print for non-interactive mode D) --headless
Answer: C - -p or --print runs Claude Code in non-interactive mode, essential for CI/CD automation.
________________________________________
Q242: Your PR review workflow needs structured JSON output for automated comment posting. Which flags combination is correct?
A) --print --json B) --print --output-format json --json-schema findings_schema.json C) --batch --format json D) --ci --structured-output
Answer: B - Non-interactive (--print), JSON format (--output-format json), schema enforcement (--json-schema) for structured CI output.
________________________________________
Q243: Your CI review analyzes a PR, provides feedback, then the developer pushes new commits. You re-run the review. How do you avoid duplicate comments on issues already flagged?
A) Use --resume to continue previous session B) Include previous review findings in context, instruct Claude to report only NEW or still-unaddressed issues C) Use fork_session D) Clear previous findings
Answer: B - Each CI run is a fresh invocation. Include prior findings in context with instructions to report only new/unaddressed issues. Prevents duplicate comments.
________________________________________
Q244: Your CI pipeline runs three review passes: security scan, code quality check, test coverage analysis. Should these be:
A) One Claude Code invocation with all three concerns B) Three separate invocations: security pass (focused security context), quality pass (focused quality context), coverage pass (focused coverage context) C) Sequential in one session D) Parallel with shared context
Answer: B - Separate focused passes: each with appropriate context, tools, and evaluation criteria. Clear separation of concerns.
________________________________________
Q245: Your automated review posts findings as PR comments. Developers report some findings are duplicates of issues from previous reviews that were fixed. What's the issue?
A) Claude doesn't remember previous reviews B) Context doesn't include which issues were addressed in recent commits; should include: prior findings + which were fixed + current code state C) Review criteria changed D) Different Claude instance
Answer: B - Context must include: previous findings, what was fixed, current code. Enables identification of new vs persistent vs resolved issues.
________________________________________
Q246: Your CI review workflow: code review → test generation → security scan. Each step generates verbose output. Context management strategy?
A) Use 200k context model B) Progressive summarization: code review → extract key findings → test generation uses findings (not full review) → security scan uses findings C) Reduce output verbosity D) Separate CI jobs without shared context
Answer: B - Progressive summarization between stages: each stage consumes summaries from previous stages, not full verbose output.
________________________________________
Q247: Your PR review identifies 30 issues in a 20-file PR. Developers are overwhelmed. How should findings be prioritized?
A) Report all issues equally B) Structured severity levels (critical/high/medium/low) with explicit criteria; developers can filter by severity C) Report only critical issues D) Random sampling
Answer: B - Structured severity with explicit criteria enables prioritized response. Critical issues addressed first.
________________________________________
Q248: Your CI pipeline runs on every commit (could be 50+ per day per developer). Cost is becoming significant. What's the optimization?
A) Run less frequently B) Implement smart triggering: lightweight checks on every commit, comprehensive review only on PR creation or when certain files change C) Reduce review scope D) Switch to cheaper model
Answer: B - Tiered checks: quick checks frequently, expensive comprehensive reviews at key points (PR creation, certain file changes).
________________________________________
Q249: Your automated code review agent has access to: Read (file access), Bash (run tests), Grep (search), and Write (generate fixes). Should Write be available in CI?
A) Yes, for automatic fixes B) No, CI review should be read-only (Read, Bash, Grep only) for safety; fixes require human approval C) Yes, with approval D) Depends on issue type
Answer: B - CI review should be read-only for safety. Generates feedback and suggestions; human developers apply fixes.
________________________________________
Q250: Your CI review needs to provide actionable feedback developers can act on immediately. Output structure should include:
A) Just issue descriptions B) Structured findings: issue description, file/line location, severity, specific code example, suggested fix, explanation of why it's problematic C) List of issues D) Pass/fail only
Answer: B - Actionable feedback requires: what's wrong, where, how bad, what to do, why it matters. Enables efficient fixing.
________________________________________
Q251: Your CI pipeline reviews PRs with 1-30 files. Single-pass review of large PRs produces inconsistent quality. What's the architectural solution?
A) Increase model size B) Multi-pass: per-file local analysis pass (consistent depth), then cross-file integration pass (architectural consistency, interface coherence) C) Review only changed lines D) Detailed instructions
Answer: B - Multi-pass architecture: focused per-file passes (consistent depth), separate integration pass (cross-file issues).
________________________________________
Q252: Your PR review posts inline comments on specific lines. To do this, the review output must include file paths and line numbers. How is this structured?
A) Natural language mentions of files B) Structured output: [{"file": "src/auth.js", "line": 42, "issue": "...", "severity": "high", "suggestion": "..."}] C) Just issue descriptions D) Diff format
Answer: B - Structured output with file paths and line numbers enables automated inline comment posting.
________________________________________
Q253: Your CI review generates many findings. Some are false positives (flagging acceptable patterns). How do you reduce false positives?
A) Lower sensitivity B) Explicit criteria defining what IS and ISN'T a problem with few-shot examples showing acceptable patterns vs genuine issues C) Post-review filtering D) Developer voting
Answer: B - Explicit criteria with concrete examples showing acceptable patterns reduces false positives by establishing clear boundaries.
________________________________________
Q254: Your CI system needs to track review findings over time (trends, frequently flagged issues, resolution rates). Output format should be:
A) Human-readable text B) Structured JSON with consistent schema, unique issue IDs, timestamps, enabling aggregation and trend analysis C) HTML reports D) Log files
Answer: B - Structured, consistent output enables automated tracking, metrics, and trend analysis over time.
________________________________________
Q255: Your PR review running in CI should escalate when:
A) Any issue is found B) Critical security vulnerabilities are detected, or architectural changes require human judgment C) More than 10 issues found D) Developer requests it
Answer: B - Escalate for: security issues requiring immediate attention, architectural decisions beyond code quality scope.
________________________________________
Q256: Your CI review workflow processes PRs in a monorepo with 20 services. A PR changes 2 services. Should the review:
A) Analyze entire codebase B) Analyze only changed files and their direct dependencies C) Analyze changed services completely D) Analyze random sampling
Answer: B - Scope to changed files + immediate dependencies. Balances thorough review with efficiency.
________________________________________
Domain 2: Tool Design & MCP Integration (11 questions)
Q257: Your CI review MCP server provides: get_pr_files, get_git_diff, run_tests, check_linting, check_security, post_comment, approve_pr, request_changes. A review agent should have access to:
A) All tools B) Read-only tools: get_pr_files, get_git_diff, run_tests, check_linting, check_security (NOT post_comment, approve_pr, or request_changes - those are workflow actions) C) Just get_pr_files D) Depends on PR size
Answer: B - Review agent gets read-only tools for analysis. Workflow actions (commenting, approving) handled by CI orchestration based on agent findings.
________________________________________
Q258: Your run_tests MCP tool in CI can fail for various reasons: tests failed, syntax error, timeout, environment issue. How should errors be structured?
A) Return exit code B) Structured: {"isError": true, "errorCategory": "test_failure|syntax|timeout|environment", "failedTests": [...], "isRetryable": bool, "errorDetails": "...", "logs": "..."} C) Just stdout/stderr D) Exception with message
Answer: B - Structured errors enable intelligent handling: test failures (need code fixes), environment issues (need CI fixes), timeouts (need optimization).
________________________________________
Q259: Your get_git_diff tool returns complete diffs (could be 1000s of lines for large PRs). The review agent often only needs diff summaries. What's the optimization?
A) Always return complete diff B) Two modes: get_diff_summary (files changed, lines added/deleted) and get_diff_detailed (complete diff) C) Compress diff D) Truncate large diffs
Answer: B - Different detail levels: summary for overview/routing, detailed for line-by-line analysis.
________________________________________
Q260: Your CI MCP server should be configured:
A) User-level (~/.claude.json) B) Project-level (.mcp.json) so all team members/CI runners have consistent tool access C) System-level D) In CI scripts
Answer: B - Project-level MCP config for consistent CI tooling across team and CI environment.
________________________________________
Q261: Your MCP tool check_security scans for vulnerabilities. It returns many findings, but most are low-severity. CI review highlights all findings equally. What's the fix?
A) Reduce sensitivity B) Tool returns structured output with severity levels: [{"vulnerability": "...", "severity": "critical|high|medium|low", "file": "...", "line": number, "cvss_score": number}]; review focuses on critical/high C) Security tool should filter D) Manual review only
Answer: B - Structured severity enables intelligent prioritization. Critical/high findings get immediate attention.
________________________________________
Q262: Your CI review tool descriptions for check_style and check_quality overlap, causing confusion. How should they be differentiated?
A) Rename them B) Expand descriptions: "check_style: Formatting, naming conventions, cosmetic issues. check_quality: Logic complexity, code smells, maintainability concerns." C) Merge into one tool D) Use both always
Answer: B - Clear differentiation: style (cosmetic, formatting) vs quality (logic, maintainability).
________________________________________
Q263: Your get_pr_files tool can fail: API rate limit, authentication error, PR not found, network timeout. How should errors be communicated?
A) Return error strings B) Structured: {"isError": true, "errorCategory": "rate_limit|auth|not_found|network", "isRetryable": bool, "retryAfter": seconds, "alternativeAction": "..."} C) HTTP status codes D) Exceptions
Answer: B - Structured errors with retry guidance: rate limits (wait and retry), auth (CI config issue), not found (skip), network (retry).
________________________________________
Q264: Your CI has MCP tools for multiple SCM systems (GitHub, GitLab, Bitbucket). Should there be one tool or separate tools per platform?
A) Separate tools per platform B) Unified tool with platform detection: get_pr_files works with any platform, internally detects platform from environment C) Manual selection D) Different agents per platform
Answer: B - Unified interface with platform abstraction simplifies agent logic. Internal implementation handles platform differences.
________________________________________
Q265: Your run_tests tool output includes verbose test framework output (100s of lines). CI review only needs summary + failed test details. Optimization?
A) Full output always B) Tool implementation parses test output, returns structured summary: {"total": 150, "passed": 148, "failed": 2, "failedTests": [{"name": "...", "error": "...", "file": "...", "line": number}]} C) Use different test framework D) Compression
Answer: B - Tool-level output parsing: return structured, relevant information (summary + failures), not raw verbose output.
________________________________________
Q266: Your CI review MCP tool post_finding_comment posts inline PR comments. It should include:
A) Just the finding description B) Structured input: {"file": "...", "line": number, "severity": "...", "issue": "...", "suggestion": "...", "explanation": "..."} C) Simple text D) Auto-generated ID only
Answer: B - Structured input for inline comments: location (file, line), severity, issue, suggestion, explanation. Complete actionable feedback.
________________________________________
Q267: Your CI environment has strict resource limits (memory, CPU, timeout). MCP tools should:
A) Use maximum resources for thorough analysis B) Have configurable resource limits: timeout parameters, memory limits, scope restrictions for resource-constrained environments C) Fail if resources insufficient D) Run indefinitely
Answer: B - Configurable resource limits enable CI tools to respect environment constraints while providing best-effort analysis.
________________________________________
Domain 3: Claude Code Configuration & Workflows (12 questions)
Q268: Your CI pipeline needs consistent review criteria across all PRs. Where should review criteria (security standards, code quality metrics, testing requirements) be configured?
A) In CI scripts B) In .claude/CLAUDE.md in the repository, automatically available to CI-invoked Claude Code C) Hard-coded in prompts D) Separate configuration file
Answer: B - Project-level CLAUDE.md provides consistent context for CI reviews. Version-controlled with code.
________________________________________
Q269: Your team's coding standards evolve over time. When standards change, how are CI reviews updated?
A) Manually update CI scripts B) Update .claude/CLAUDE.md in repository; CI automatically uses updated standards on next run C) Reconfigure CI system D) Email team
Answer: B - Standards in version-controlled CLAUDE.md mean CI reviews automatically use current standards.
________________________________________
Q270: Your CI runs on different branch types: main (strict reviews), feature/* (moderate reviews), experimental/* (lenient reviews). How should this be configured?
A) Different CI jobs with different configurations B) CLAUDE.md or rules can include branch-specific guidance, or CI passes branch context in prompt C) Manual review level selection D) Not possible
Answer: B - Branch context in CLAUDE.md or passed in CI prompt enables context-appropriate review rigor.
________________________________________
Q271: Your CI review flagged a pattern as problematic, but it's actually acceptable per your team's conventions (documented in style guide). How do you prevent future false positives?
A) Filter the specific finding B) Add the acceptable pattern to CLAUDE.md with explanation: "This pattern is acceptable because [reason]" with example C) Ignore security findings D) Change the code
Answer: B - Document acceptable patterns in CLAUDE.md to establish clear boundaries, reducing false positives.
________________________________________
Q272: Your monorepo has 10 services with different conventions. Service A uses TypeScript + React, Service B uses Python + Flask. CI reviews need service-appropriate standards. Configuration?
A) One CLAUDE.md with all standards B) .claude/rules/ with path-specific patterns: service-a.md with paths: ["services/service-a/**"], service-b.md with paths: ["services/service-b/**"] C) Separate repositories D) Manual service specification
Answer: B - Path-specific rules automatically apply service-appropriate standards based on file location in monorepo.
________________________________________
Q273: Your team's security requirements (no hardcoded credentials, input validation, SQL injection prevention) apply to all PRs. Where should these be configured?
A) CI scripts B) In .claude/CLAUDE.md security section, always included in CI review context C) External security tool D) Wiki
Answer: B - Critical security requirements in CLAUDE.md ensure consistent enforcement across all CI reviews.
________________________________________
Q274: Your CI runs Claude Code in non-interactive mode with --print. The command needs context about: testing standards, code quality criteria, security requirements. Where does this context come from?
A) Command-line arguments B) .claude/CLAUDE.md in the repository (automatically loaded) + prompt specifying the review task C) Environment variables D) Separate config file
Answer: B - CLAUDE.md provides persistent context, prompt specifies the specific review task. Combined for complete CI context.
________________________________________
Q275: Your CI review generates findings, but developers want to know which coding standards each finding relates to. Output structure should include:
A) Just the finding B) Finding with standard reference: {"issue": "...", "standard": "PEP 8, Section 4.1", "severity": "...", "suggestion": "..."} C) Standard ID only D) No reference needed
Answer: B - Standard references enable developers to look up reasoning, learn standards, understand context.
________________________________________
Q276: Your CI pipeline reviews PRs but doesn't have access to private documentation (internal APIs, architecture decisions). How should this be handled?
A) CI reviews are limited B) Include essential internal documentation in CLAUDE.md (if safe to commit) or make available via MCP tools with authentication C) Manual review only D) Skip reviews needing internal context
Answer: B - Internal documentation in CLAUDE.md (if not sensitive) or via authenticated tools provides necessary context for thorough reviews.
________________________________________
Q277: Your CI runs on PRs from external contributors (public repo). CLAUDE.md includes internal details. Is this appropriate?
A) Yes, needed for quality reviews B) Consider: CLAUDE.md in public repos is visible to external contributors; sensitive information should not be included C) External PRs shouldn't be reviewed D) Use separate CLAUDE.md for external PRs
Answer: B - Security consideration for public repos: CLAUDE.md is visible to everyone. No sensitive internal information.
________________________________________
Q278: Your CI review CLI command structure is:
bash
claude --print "Review this PR for: [criteria]" --output-format json --json-schema schema.json
Where do [criteria] come from?
A) Hard-coded in script B) Combination of: CLAUDE.md (persistent standards) + CI script prompt (specific review task/focus) C) User input D) Random selection
Answer: B - CLAUDE.md provides foundational standards, CI prompt specifies specific review focus (security scan vs performance review, etc.).
________________________________________
Q279: Your CI generates JSON findings that are posted as PR comments. The findings schema should include:
A) Just issue descriptions B) Complete finding info: {"id": "unique-id", "file": "...", "line": number, "severity": "...", "category": "security|quality|style", "issue": "...", "suggestion": "...", "standard": "..."} C) Pass/fail only D) Random findings
Answer: B - Complete structured findings with unique IDs, location, categorization, actionable suggestions enable effective CI integration.
________________________________________
Domain 4: Prompt Engineering & Structured Output (12 questions)
Q280: Your CI review produces inconsistent findings - the same code pattern flagged in one PR, ignored in another. What improves consistency?
A) Random seed setting B) Explicit criteria with few-shot examples: "Flag: [pattern example]. Don't flag: [acceptable variation example]." Establishes clear, consistent boundaries. C) Multiple reviews and voting D) Different model
Answer: B - Explicit criteria with concrete examples establish consistent evaluation standards across PRs.
________________________________________
Q281: Your CI security scan needs to output findings in a specific format for security dashboard ingestion. How do you enforce format?
A) Detailed formatting instructions B) Define security_scan tool with JSON schema specifying exact structure: [{"vulnerability_id": "...", "severity": "...", "cvss_score": number, "description": "...", "remediation": "...", "file": "...", "line": number}] C) Post-processing D) Security tool handles formatting
Answer: B - JSON schema via tool_use enforces exact output structure for downstream system integration.
________________________________________
Q282: Your code quality review rates files (1-5) but ratings are inconsistent. What improves consistency?
A) Simplify to pass/fail B) Explicit rating criteria with few-shot examples for each level: "5: Clean, tested, documented [code example]. 3: Functional, needs improvement [code example]. 1: Critical issues [code example]." C) Average multiple ratings D) Use static analysis scores
Answer: B - Explicit criteria with concrete code examples for each rating level establish consistent standards.
________________________________________
Q283: Your CI review generates too many false positives (flagging acceptable code). Developers ignore findings. How do you reduce false positives?
A) Lower overall sensitivity B) Explicit criteria defining genuine issues with few-shot examples showing: "Genuine issue: [example]. Acceptable pattern: [example]." C) Developer voting D) Post-review filtering
Answer: B - Show both positive (genuine issues) and negative (acceptable patterns) examples to establish clear boundaries.
________________________________________
Q284: Your CI review needs to categorize findings: security, performance, maintainability, style. This enables filtering and prioritization. How is this enforced?
A) Natural language categories B) Structured output with enum: {"finding": "...", "category": "security|performance|maintainability|style", "severity": "...", "suggestion": "..."} C) Free-text categories D) Multiple output files
Answer: B - Structured categorization via enum enables reliable filtering, routing, and prioritization.
________________________________________
Q285: Your CI generates test cases for new code. Tests need consistent structure (arrange-act-assert) and naming. What ensures consistency?
A) Testing framework enforces this B) Few-shot examples in CLAUDE.md showing ideal test structure: setup, execution, assertion, teardown with proper naming conventions C) Template files D) Manual review
Answer: B - Few-shot examples demonstrate test patterns (structure, naming, organization) for consistent generation.
________________________________________
Q286: Your CI review of a 20-file PR produces superficial analysis of some files. What's the architectural solution?
A) Longer timeout B) Multi-pass review: per-file local analysis (consistent depth on each file), then cross-file integration pass (architectural consistency) C) Review fewer files D) Larger model
Answer: B - Multi-pass architecture addresses attention dilution: focused per-file passes + integration pass.
________________________________________
Q287: Your CI needs to track findings over time (which issues recur, which get fixed quickly). Output structure should include:
A) Just current findings B) Structured output with: {"finding_id": "unique-hash-based-on-issue", "first_seen": "PR#123", "status": "new|recurring|fixed", "file": "...", "issue": "..."} C) Text findings only D) No tracking needed
Answer: B - Unique, consistent finding IDs enable tracking across PRs: recurrence patterns, resolution speed, trend analysis.
________________________________________
Q288: Your CI review posts findings as PR comments. Some findings need code suggestions, others just explanations. Output structure should:
A) Always include code suggestions B) Structured with optional suggestion: {"issue": "...", "severity": "...", "explanation": "...", "suggested_fix": "optional-code", "affected_code": "..."} C) Text only D) Suggestions in separate output
Answer: B - Flexible structure: explanation always, code suggestion optional when applicable. Enables varied finding types.
________________________________________
Q289: Your CI generates many findings per PR. Developers are overwhelmed. How should findings be prioritized?
A) Random order B) Structured severity with explicit prioritization: {"severity": "critical|high|medium|low", "impact": "security|correctness|performance|style", "priority": number} enabling filtering by criticality C) Alphabetical D) All equal priority
Answer: B - Multi-dimensional prioritization (severity, impact category, priority score) enables developers to focus on critical issues first.
________________________________________
Q290: Your CI review must distinguish between: blocking issues (must be fixed before merge) and advisory issues (suggestions for improvement). How is this structured?
A) Severity levels handle this B) Explicit blocking flag: {"issue": "...", "severity": "...", "blocking": true|false, "rationale": "why this blocks merge"} C) Separate output files D) All issues are advisory
Answer: B - Explicit blocking designation enables automated merge gating: blocking issues prevent merge, advisory issues are suggestions.
________________________________________
Q291: Your CI review generates findings, but developers want examples of how to fix issues. Output structure should include:
A) Just issue description B) Complete finding: {"issue": "...", "current_code": "...", "suggested_fix": "...", "explanation": "why the fix is better", "references": ["standard", "documentation"]} C) Issue only, developers research fixes D) Auto-apply fixes
Answer: B - Actionable findings: what's wrong, current code, suggested fix, explanation, references. Enables efficient fixing.
________________________________________
Domain 5: Context Management & Reliability (9 questions)
Q292: Your CI reviews PRs with 1-30 files. For 30-file PRs, the review's quality deteriorates on later files (less thorough, missing issues). What's the issue?
A) Too many files B) Single-pass review causes attention dilution; later files receive less thorough analysis C) Timeout pressure D) Model fatigue
Answer: B - Attention dilution in large single-pass reviews. Solution: multi-pass architecture (per-file + integration).
________________________________________
Q293: Your CI review needs to reference team standards (coding conventions, architecture patterns, security requirements). These are detailed. How should they be provided?
A) All in prompt B) Standards in CLAUDE.md (persistent, version-controlled), prompt references specific relevant sections C) External links D) Memory from previous reviews
Answer: B - Standards in CLAUDE.md provide persistent context. Prompt can reference specific sections for focused reviews.
________________________________________
Q294: Your CI review runs on every push to PR. Early reviews produce findings, developer fixes some issues, pushes changes. Next review should:
A) Review everything from scratch B) Include prior findings in context: "Previous review found [issues]. Developer fixed [these]. Current review focus: new issues and unfixed previous issues." C) Only review changed lines D) Skip reviewed files
Answer: B - Context includes review history: what was found, what was fixed, focus on new/remaining issues. Prevents duplicates.
________________________________________
Q295: Your CI runs run_tests which outputs 500 lines of test results. The review only needs: passed/failed count and failed test details. What's the optimization?
A) Full output for completeness B) Parse test output in tool implementation, return structured summary: {"passed": 487, "failed": 3, "failed_tests": [{"name": "...", "error": "...", "file": "...", "line": number}]} C) Post-process to trim D) Use different test runner
Answer: B - Tool-level output optimization: parse verbose output, return structured relevant information.
________________________________________
Q296: Your CI review of a PR with 100 commits and 25 files produces a token count warning (context 95% full). What's the issue?
A) Too many commits B) Accumulated file contents, diff information, and test results fill context without summarization C) Model limitation D) Review too thorough
Answer: B - Large PRs accumulate context without management. Need: selective file reading, diff summaries, progressive analysis.
________________________________________
Q297: Your CI review must escalate when it detects:
A) Any code quality issue B) Critical security vulnerabilities, or architectural changes beyond code review scope (require human architectural review) C) More than 10 findings D) Complex code
Answer: B - Escalate for: critical security (immediate action needed), architectural decisions (require human judgment beyond code review).
________________________________________
Q298: Your CI reviews fail intermittently with "context length exceeded" on medium-sized PRs (10-15 files). What's the likely cause and solution?
A) Model limitations; use larger model B) Accumulating full file contents instead of focused reading; optimize to read only relevant sections or use progressive summarization C) PRs are too large D) Review is too detailed
Answer: B - Context management issue: read full files unnecessarily. Solution: targeted reading (only changed sections + context), summarization.
________________________________________
Q299: Your CI review generates findings but occasionally references code that doesn't exist in the PR (hallucination). How do you reduce this?
A) Lower temperature B) Ground review in actual code: include file contents in context, instruct to quote actual code in findings, require line numbers C) More examples D) Different model
Answer: B - Grounding: provide actual code, require findings to reference actual code with line numbers. Reduces hallucination.
________________________________________
Q300: Your CI review runs on PRs from junior and senior developers. Junior dev PRs have more findings. How should findings be calibrated?
A) Same standards for everyone B) Consistent standards but calibrated expectations: clear severity criteria ensure junior devs get constructive feedback without being overwhelmed; focus on critical issues first C) Easier review for junior devs D) Senior devs skip review
Answer: B - Consistent standards (don't lower quality expectations) but structured severity/prioritization helps all developers focus on what matters most.
________________________________________
________________________________________
SCENARIO 6: Structured Data Extraction (60 Questions)
You are building a structured data extraction system using Claude. The system extracts information from unstructured documents (invoices, receipts, contracts, emails), validates the output using JavaScript Object Notation (JSON) schemas, and maintains high accuracy. It must handle edge cases gracefully and integrate with downstream systems.
________________________________________
Domain 1: Agentic Architecture & Orchestration (16 questions)
Q301: Your extraction system processes documents. The agentic loop should:
A) Extract once and return results B) Extract → validate → if validation fails, retry with specific error feedback → repeat until valid or max retries C) Extract and hope for accuracy D) Extract multiple times and vote
Answer: B - Validation-retry loop: extract, validate, provide specific feedback on errors, retry with corrections. Improves accuracy through self-correction.
________________________________________
Q302: Your extraction system processes invoices with line items. After extraction, validation reveals the line item totals don't sum to the subtotal. The retry prompt should include:
A) Just the invoice again B) Invoice + failed extraction + specific validation error: "Line items sum to $245.50 but subtotal shows $245.00. Verify line item prices and quantities." C) Just the error D) A different extraction strategy
Answer: B - Effective retry includes: source document, what was attempted (failed extraction), specific error feedback. Enables targeted correction.
________________________________________
Q303: Your extraction system processes 100 invoices per day. Processing is overnight batch work. What's the optimal API approach?
A) Synchronous Messages API B) Message Batches API for 50% cost savings with up to 24-hour processing window C) Parallel synchronous calls D) Streaming API
Answer: B - Message Batches API for non-blocking, latency-tolerant batch processing. Significant cost savings for overnight work.
________________________________________
Q304: You submit 100 invoices to Message Batches API. After processing, 7 failed: 4 timeouts, 3 format errors. Retry strategy?
A) Retry all identically B) Retry 4 timeouts (transient); analyze 3 format errors to determine if documents need preprocessing (OCR, format conversion) before retry C) Discard failures D) Resubmit entire batch
Answer: B - Error-aware retry: transient errors (retry immediately), format errors (may need preprocessing or may be unprocessable).
________________________________________
Q305: Your extraction handles various invoice formats: tabular, prose, handwritten (OCR'd). Extraction quality varies by format. How should this be structured architecturally?
A) One extraction approach for all formats B) Format detection → route to format-specific extraction → unified output schema C) Best-effort extraction without format consideration D) Manual format specification
Answer: B - Format-aware pipeline: detect format (table/prose/handwritten), route to appropriate extraction strategy, normalize to common schema.
________________________________________
Q306: Your extraction system occasionally returns placeholders like "[AMOUNT NOT FOUND]" instead of null for missing fields. This causes downstream errors. What's the issue?
A) Agent is being helpful B) Required fields in schema force fabrication when data is absent; make fields optional/nullable to allow null instead of placeholders C) Temperature too high D) Insufficient retries
Answer: B - Required fields pressure model to fabricate data. Optional/nullable fields allow honest null when data is missing.
________________________________________
Q307: Your extraction system processes financial documents. Accuracy is critical. What's the appropriate agentic workflow?
A) Single extraction pass B) Extract → validate business rules (totals sum, dates consistent) → if validation fails, retry with specific error → verify critical fields → flag low-confidence extractions for human review C) Extract and trust D) Multiple extractions, pick most common
Answer: B - Multi-stage validation workflow: extraction, business rule validation, retry loop, confidence-based routing. Critical systems need defensive validation.
________________________________________
Q308: Your extraction system must handle documents where some required information is genuinely absent (e.g., invoice has no tax line). How should this be designed?
A) Required fields for everything B) Fields that may be absent should be optional/nullable in schema; distinguish between "field not found" (null) and "field found with value" (actual value) C) Use default values D) Reject documents with missing fields
Answer: B - Optional/nullable fields for potentially absent information. Prevents fabrication, enables honest reporting of missing data.
________________________________________
Q309: Your extraction processes receipts where some have poor image quality (blurry, low contrast). Extraction accuracy is lower on poor-quality images. How should quality be handled?
A) Reject poor-quality images B) Extract with confidence scoring; route low-confidence extractions to human review; return extraction with quality metadata: {"extraction": {...}, "image_quality": "high|medium|low", "confidence": 0.75} C) Pre-process all images to enhance quality D) Ignore quality issues
Answer: B - Quality-aware extraction: extract with confidence scores, quality metadata enables intelligent routing (low quality → human review).
________________________________________
Q310: Your extraction system processes contracts (long, complex documents). Full document processing exceeds context limits. What's the architectural approach?
A) Use 200k context model B) Section-based extraction: split document into sections (parties, terms, obligations, dates), extract each section independently, merge structured results C) Summarize document first D) Extract key pages only
Answer: B - Divide-and-conquer: section-based extraction with structured outputs, merge results. Handles long documents within context limits.
________________________________________
Q311: Your extraction system must preserve provenance (which document each extracted value came from, which page/section). How should this be structured?
A) Extraction values only B) Structured extraction with source attribution: {"field": "invoice_number", "value": "INV-12345", "source_doc": "invoice_march.pdf", "page": 1, "confidence": 0.95} C) Just values D) Separate provenance file
Answer: B - Structured extraction with provenance metadata enables audit trails, source verification, troubleshooting.
________________________________________
Q312: Your extraction system processes forms where the same field (e.g., "Name") appears multiple times (customer name, shipping name, billing name). Extraction occasionally confuses these. How should this be addressed?
A) Better extraction prompts B) Structured extraction with context: {"customer_name": "John Smith", "shipping_name": "Jane Smith", "billing_name": "John Smith"} with clear field differentiation C) Extract all names generically D) Manual review
Answer: B - Context-aware extraction with clearly differentiated fields. Schema design prevents ambiguity.
________________________________________
Q313: Your extraction system handles documents in English, Spanish, and French. Extraction accuracy varies by language. How should this be architecturally handled?
A) One extraction approach B) Language detection → language-aware extraction (language-specific prompts, examples) → unified output schema C) English only D) Human translation first
Answer: B - Language-aware pipeline: detect language, apply language-specific extraction approach (examples in that language), normalize to common output schema.
________________________________________
Q314: Your extraction system processes invoices but occasionally extracts data from the wrong invoice when multiple invoices are in one PDF. How should this be prevented?
A) Better extraction prompts B) Document separation: detect multi-invoice PDFs, split into individual invoices, extract each independently with document ID tracking C) Extract first invoice only D) Manual separation
Answer: B - Document separation preprocessing: split multi-document files, extract each with tracking, prevents cross-contamination.
________________________________________
Q315: Your extraction system must handle edge cases: handwritten notes, stamps over text, complex tables, non-standard formats. These fail extraction. How should failures be handled?
A) Reject edge cases B) Graceful degradation: attempt extraction, return partial results with quality/confidence indicators, flag for human review, include error context: "Complex table structure made extraction uncertain" C) Skip edge cases D) Force extraction
Answer: B - Graceful degradation with context: attempt extraction, partial results, quality indicators, human review routing, error context.
________________________________________
Q316: Your extraction system processes thousands of documents daily. You need to track extraction performance over time (accuracy, failure rates, processing time). What's required?
A) Manual tracking B) Structured extraction logging with metrics: document ID, extraction time, confidence scores, validation results, errors, enabling automated performance monitoring C) Sample checking D) User feedback only
Answer: B - Structured logging with consistent metrics enables automated monitoring, trend analysis, performance tracking.
________________________________________
Domain 2: Tool Design & MCP Integration (11 questions)
Q317: Your extraction MCP server provides tools: extract_invoice, extract_receipt, extract_contract, extract_form. These are similar (all extract structured data from documents). Should they be separate tools?
A) Yes, separate tools for each document type with type-specific schemas and examples B) One generic extract_document tool C) Combine invoices and receipts, separate contracts D) User choice
Answer: A - Document-type-specific tools with tailored schemas and examples improve extraction accuracy. Each document type has different structure and fields.
________________________________________
Q318: Your extraction MCP tool can fail for various reasons: unreadable document, unsupported format, extraction timeout, validation failure. How should errors be structured?
A) Generic error messages B) Structured: {"isError": true, "errorCategory": "unreadable|format_unsupported|timeout|validation_failed", "isRetryable": bool, "partialExtraction": {...}, "suggestion": "..."} C) HTTP codes D) Exceptions
Answer: B - Structured errors with categorization: unreadable (OCR issue), unsupported format (preprocessing needed), timeout (simplify), validation (retry with feedback).
________________________________________
Q319: Your extract_invoice tool returns structured data with 40 fields (all invoice details). Downstream system only needs 8 fields. What's the optimization?
A) Extract all 40 fields always B) Two tools: extract_invoice_summary (8 essential fields) and extract_invoice_detailed (all 40 fields); use summary for most cases C) Let downstream filter D) Configure fields per request
Answer: B - Different detail levels: summary for routine processing, detailed for comprehensive needs. Optimizes processing and context usage.
________________________________________
Q320: Your extraction system's MCP server should be configured:
A) User-level for personal extractions B) Project-level so all team members/services have consistent extraction tools C) Mixed D) Ad-hoc
Answer: B - Project-level MCP config for consistent extraction tooling across team and services.
________________________________________
Q321: Your extraction MCP tool extract_invoice should return:
A) Just extracted values B) Structured output with extraction and metadata: {"extraction": {invoice_number, date, items...}, "metadata": {"confidence": 0.92, "processing_time_ms": 1250, "validation_passed": true}} C) Extraction only D) Raw text
Answer: B - Extraction + metadata: confidence enables routing decisions, validation status indicates reliability, processing time tracks performance.
________________________________________
Q322: Your extraction tools process documents that may contain personal information (PII). How should this be handled in tool design?
A) Extract everything B) Tools should have PII detection and optional redaction: extract_document(..., redact_pii: bool) with PII flagging in output: {"field": "ssn", "value": "***-**-1234", "pii_redacted": true} C) Ignore PII D) Manual redaction only
Answer: B - PII-aware tools: detection, optional redaction, flagging in output. Enables privacy-compliant extraction.
________________________________________
Q323: Your extraction MCP tool validate_extraction checks if extracted data matches business rules (totals sum, dates valid, etc.). What should it return?
A) Pass/fail only B) Structured validation results: {"valid": false, "errors": [{"field": "subtotal", "error": "Line items sum to $245.50 but subtotal is $245.00", "severity": "high"}]} C) Error count D) Boolean
Answer: B - Structured validation with specific errors: which fields, what's wrong, severity. Enables targeted retry and fixes.
________________________________________
Q324: Your extraction handles multiple document formats (PDF, images, scanned docs). Should format handling be in one tool or separate tools?
A) Separate tools per format B) One unified tool with format detection and appropriate preprocessing (PDF text extraction, OCR for images, etc.) C) Manual format specification D) Format-specific services
Answer: B - Unified tool with format abstraction: detect format, apply appropriate preprocessing, consistent extraction interface. Simplifies usage.
________________________________________
Q325: Your extraction tool processes invoices but some have inconsistent terminology ("Total", "Amount Due", "Balance" all meaning the same thing). How should this be handled?
A) Strict field matching B) Semantic extraction: tool understands synonyms and variations, maps to canonical fields: {"total_amount": 250.00, "source_label": "Amount Due"} C) Reject inconsistent terminology D) Manual mapping
Answer: B - Semantic extraction with canonical field mapping. Handles terminology variations, preserves source labels for verification.
________________________________________
Q326: Your extraction system provides an MCP tool extract_batch for processing multiple documents. What should it return?
A) Array of extractions B) Structured batch results: {"processed": 100, "successful": 95, "failed": 5, "results": [{"doc_id": "...", "extraction": {...}, "status": "success|failed", "error": "..."}]} C) Success count only D) First successful extraction
Answer: B - Comprehensive batch results: summary stats, individual results with status/errors, enables tracking and selective retry.
________________________________________
Q327: Your extraction tool extract_invoice occasionally extracts data from the wrong sections (confusing header with footer, mixing line items). How should tools be improved?
A) Better prompts only B) Schema design with semantic constraints: line_items is array of objects with description, quantity, unit_price, total; validation ensures structure consistency C) Larger model D) Manual extraction
Answer: B - Well-designed schemas with semantic structure (arrays, nested objects, required relationships) guide extraction toward correct structure.
________________________________________
Domain 3: Claude Code Configuration & Workflows (12 questions)
Q328: Your extraction team uses Claude Code to develop extraction prompts and schemas. Team standards (schema design patterns, validation approaches, testing methodology) should be configured:
A) In each developer's personal notes B) In .claude/CLAUDE.md in the extraction system repository C) External wiki D) Code comments only
Answer: B - Project-level CLAUDE.md for shared extraction development standards, patterns, and methodologies.
________________________________________
Q329: Your extraction system handles different document types (invoices, receipts, contracts) with type-specific schemas and validation rules. How should this be configured for Claude Code development?
A) One CLAUDE.md with all document types B) .claude/rules/ with path-specific patterns: invoices.md with paths: ["schemas/invoices/**", "extractors/invoice_*.py"], receipts.md with paths: ["schemas/receipts/**", "extractors/receipt_*.py"] C) Separate repositories per type D) No configuration needed
Answer: B - Path-specific rules apply document-type-specific context based on file location. Developers get relevant schema/validation guidance automatically.
________________________________________
Q330: Your team's data extraction quality standards (accuracy targets, validation requirements, edge case handling) are shared across 3 extraction projects. How should this be configured?
A) Copy standards to each project B) Create standards/extraction_quality.md, use @import standards/extraction_quality.md in each project C) Wiki reference D) Verbal communication
Answer: B - @import for reusable standards. Single source of truth, imported where needed.
________________________________________
Q331: You create a custom command /validate-schema that checks JSON schema validity and completeness. Where should this be located for team availability?
A) .claude/commands/validate-schema.sh in repository B) Each developer's home directory C) System path D) As a skill
Answer: A - Project-scoped commands in version control, automatically available to all team members.
________________________________________
Q332: Your extraction system's CLAUDE.md includes example documents with actual customer data (for schema development context). Is this appropriate?
A) Yes, real examples are helpful B) No, CLAUDE.md is version-controlled; use anonymized/synthetic examples instead to avoid committing sensitive data C) Only if repository is private D) PII is okay in development
Answer: B - Security/privacy: CLAUDE.md is version-controlled. Use synthetic/anonymized examples, never real customer data.
________________________________________
Q333: Your extraction prompts need to reference field definitions, validation rules, and edge case handling. These are extensive. How should they be organized?
A) All in one CLAUDE.md file B) Modular: schemas/field_definitions.md, validation/rules.md, docs/edge_cases.md, imported into CLAUDE.md C) External documentation D) Hard-coded in prompts
Answer: B - Modular organization with imports keeps CLAUDE.md focused while providing comprehensive reference material.
________________________________________
Q334: Your extraction development team works on schema design, extraction logic, and validation rules. Team members specialize in different document types. How should context be organized?
A) One CLAUDE.md for all work B) Path-specific rules: schema developers get schema guidance when editing schemas, extraction developers get extraction patterns when editing extractors C) Manual context switching D) No organization
Answer: B - Path-specific rules provide role-appropriate and document-type-appropriate context based on what developers are working on.
________________________________________
Q335: Your extraction system's schema validation rules are complex (field relationships, business logic). How should these be documented for Claude Code development?
A) In code comments only B) In CLAUDE.md with examples: "Validation rules: 1) Line items must sum to subtotal [example]. 2) Dates must be chronological [example]. 3) Currency codes must be consistent [example]." C) Separate validation document D) Undocumented
Answer: B - Validation rules with examples in CLAUDE.md provide context for developing and testing extraction schemas.
________________________________________
Q336: Your extraction system uses Claude Code to generate test cases for new document types. Test generation should follow a specific structure (arrange-act-assert, edge cases, validation scenarios). Configuration?
A) Test template files B) .claude/rules/tests.md with testing conventions and few-shot examples showing ideal test structure for extraction scenarios C) Manual test writing D) No conventions
Answer: B - Testing conventions in path-specific rules with few-shot examples ensure consistent test generation.
________________________________________
Q337: Your extraction development uses Claude Code. Developers want to quickly test extraction schemas against sample documents. Should this be:
A) Manual testing only B) Custom skill or command: /test-extraction schema.json sample_doc.pdf that validates schema, runs extraction, reports results C) External testing tool D) Not possible in Claude Code
Answer: B - Custom commands/skills for development workflows: testing, validation, schema checking.
________________________________________
Q338: Your extraction system's CLAUDE.md has grown to 1800 lines with field definitions, validation rules, edge cases, document type specifications. Developer feedback indicates inconsistent extraction quality. What's the issue?
A) Too much detail B) Large, unfocused context; split into modular files (field_definitions.md, validation_rules.md, edge_cases.md, document_types/) with imports C) Format issues D) Developers not reading it
Answer: B - Overly long context reduces effectiveness. Modular organization with targeted imports provides relevant context without overwhelming.
________________________________________
Q339: Your extraction team uses Claude Code to refactor extraction logic. A developer is refactoring invoice extraction (affects 6 files). Should they use:
A) Direct execution: modify all files immediately B) Plan mode: analyze current logic, identify refactoring opportunities, propose approach, get review, then implement C) One file at a time without planning D) Copy-paste refactoring
Answer: B - Complex refactoring affecting multiple files warrants plan mode: analysis, design, review, then implementation.
________________________________________
Domain 4: Prompt Engineering & Structured Output (12 questions)
Q340: Your extraction system produces inconsistent output - sometimes field names are camelCase, sometimes snake_case, sometimes abbreviated. What's the most reliable solution to enforce consistent naming?
A) Detailed naming instructions B) JSON schema with exact field names: {"invoice_number": {"type": "string"}, "invoice_date": {"type": "string"}} - schema enforces exact field names C) Few-shot examples D) Post-processing renaming
Answer: B - JSON schema enforces exact field names at API level. No ambiguity, deterministic consistency.
________________________________________
Q341: You want to extract invoice line items as structured data with specific fields. How do you ensure consistent structure?
A) Detailed instructions B) JSON schema with nested structure: {"line_items": {"type": "array", "items": {"type": "object", "properties": {"description": {"type": "string"}, "quantity": {"type": "number"}, "unit_price": {"type": "number"}, "total": {"type": "number"}}, "required": ["description", "quantity", "unit_price", "total"]}}} C) Few-shot examples only D) Hope for consistency
Answer: B - JSON schema with nested arrays and objects enforces exact structure. Required fields ensure completeness.
________________________________________
Q342: Your extraction outputs dates in inconsistent formats: "2024-01-15", "Jan 15, 2024", "01/15/2024". How do you enforce consistent date format?
A) Instructions to use ISO 8601 B) JSON schema with format specification: {"invoice_date": {"type": "string", "format": "date"}} enforces YYYY-MM-DD C) Few-shot examples showing correct format D) Post-processing normalization
Answer: B - JSON schema with "format": "date" enforces ISO 8601 (YYYY-MM-DD) format. API-level enforcement.
________________________________________
Q343: Your invoice extraction schema has {"total": {"type": "number", "required": true}} but some invoices legitimately don't show totals. Extraction fails validation. What's the fix?
A) Remove validation B) Change to: {"total": {"type": ["number", "null"]}} and remove from required array, allowing null when total is absent C) Use default value of 0 D) Skip invoices without totals
Answer: B - Optional/nullable fields for data that may be legitimately absent. Prevents forced fabrication, allows honest null.
________________________________________
Q344: Your extraction includes an invoice_type field that should be one of: "invoice", "credit_note", "proforma", "quote". How do you enforce this?
A) Instructions listing valid types B) JSON schema with enum: {"invoice_type": {"type": "string", "enum": ["invoice", "credit_note", "proforma", "quote"]}} C) Few-shot examples D) Post-validation
Answer: B - Enum in JSON schema restricts values to specified list. Enforces consistency and validity.
________________________________________
Q345: Your extraction handles invoices in various currencies. Some include currency symbol ("$250.00"), others code ("USD 250.00"), others nothing. You want consistent output. Schema design?
A) Extract exactly as appears B) Separate fields: {"amount": {"type": "number"}, "currency": {"type": "string", "enum": ["USD", "EUR", "GBP", "JPY", ...], "default": "USD"}} C) String field with amount and currency D) Amount only
Answer: B - Separate normalized fields: numeric amount, standardized currency code. Enables downstream processing regardless of source format.
________________________________________
Q346: Your extraction sometimes includes hallucinated data (values that don't appear in the source document). How do you reduce hallucination?
A) Lower temperature B) Schema with source tracking: {"field": "invoice_number", "value": "INV-12345", "source_text": "Invoice # INV-12345"} enables verification; instruct to include source quotes C) More examples D) Confidence thresholds
Answer: B - Require source quotes/excerpts in extraction output. Enables verification, discourages fabrication (can't quote what doesn't exist).
________________________________________
Q347: Your extraction handles documents with varying formats (tables vs prose vs handwritten). Extraction quality varies. How do you improve consistency?
A) One approach for all B) Few-shot examples showing correct extraction from each format type: tabular invoice [example], prose invoice [example], handwritten invoice [example] C) Different prompts per format D) Best effort
Answer: B - Few-shot examples demonstrating extraction patterns across format variations. Shows how to handle structural differences.
________________________________________
Q348: Your extraction validation finds errors (line items don't sum to total). The retry prompt should:
A) Repeat the entire document B) Include: document + failed extraction + specific error: "Line 1 ($50) + Line 2 ($75) = $125, but extracted subtotal is $120. Verify line item amounts." C) Just the error message D) Try different extraction method
Answer: B - Effective retry: source document, failed attempt, specific error with calculation details. Enables targeted correction.
________________________________________
Q349: Your extraction system needs to handle edge cases: partial documents, poor image quality, uncommon formats. These have lower accuracy. How should this be surfaced?
A) Same confidence for all extractions B) Schema includes confidence/quality fields: {"extraction": {...}, "confidence": {"overall": 0.85, "per_field": {"invoice_number": 0.95, "date": 0.90, "total": 0.70}}, "image_quality": "medium"} C) Extract or fail D) Flag all edge cases for review
Answer: B - Field-level confidence scores + quality indicators enable intelligent downstream routing (low confidence → human review).
________________________________________
Q350: Your extraction generates structured output but occasionally puts data in wrong fields (customer name in company field, date in invoice number field). How do you reduce these errors?
A) Better instructions B) Schema with field descriptions and type constraints: {"customer_name": {"type": "string", "description": "Individual person's full name (e.g., John Smith)"}, "company_name": {"type": "string", "description": "Business/organization name (e.g., Acme Corp)"}} C) Few-shot examples only D) Post-validation swapping
Answer: B - Rich schema descriptions + type constraints help model understand field semantics. Reduces field confusion.
________________________________________
Q351: Your extraction outputs boolean fields (e.g., "paid" status). Sometimes boolean values are inconsistent (true, True, "true", "yes", 1). Schema enforcement?
A) Accept any truthy value B) JSON schema: {"paid": {"type": "boolean"}} enforces JSON boolean (true/false only) C) Instructions D) Post-processing normalization
Answer: B - JSON schema type "boolean" enforces JSON booleans (true/false). No string representations or numeric values.
________________________________________
Domain 5: Context Management & Reliability (9 questions)
Q352: Your extraction system processes 10-page invoices with complex tables. Full document context exceeds limits. What's the approach?
A) Use 200k context model B) Page-by-page extraction: extract essential info from each page, accumulate structured findings, merge at end C) Extract first page only D) Summarize document first
Answer: B - Chunked extraction: process in manageable sections, accumulate structured data, merge results. Handles long documents within limits.
________________________________________
Q353: Your extraction validation-retry loop sometimes loops indefinitely (validation fails, retry fails, repeat). What's the safety pattern?
A) No loop limit B) Max retry limit (e.g., 3 attempts) + distinguish retryable vs non-retryable errors: if error is non-retryable (information absent) or max retries exceeded, return partial extraction with error context C) Loop until success D) No retries
Answer: B - Bounded retry with error classification: retry transient/fixable errors up to limit, recognize non-retryable errors (missing information), return partial results + context.
________________________________________
Q354: Your extraction system processes receipts where line items can be 1-50 items. Full line item extraction for 50-item receipts fills context. What's the optimization?
A) Always extract all items B) Tiered extraction: for receipts with >20 items, extract summary (item count, categories, total) + top N items by value; detailed extraction available on request C) Limit to 10 items D) Skip large receipts
Answer: B - Adaptive extraction depth: summaries for large documents, detail for small/medium. Balances completeness with efficiency.
________________________________________
Q355: Your extraction must preserve provenance (which document, which page, source text excerpt). Context management for this metadata?
A) Metadata wastes tokens B) Structured extraction with provenance fields: {"field": "value", "source_page": 1, "source_excerpt": "relevant text"} - concise metadata enables verification without excessive context C) Full page content for every field D) No provenance
Answer: B - Concise provenance metadata (page number, brief excerpt) enables verification without excessive context consumption.
________________________________________
Q356: Your extraction processes documents in batches of 100. After 60 documents, extraction quality decreases (lower confidence, more validation failures). What's the issue?
A) Model fatigue B) Accumulating context from previous extractions without clearing; each extraction should have fresh context C) Documents getting harder D) Need break
Answer: B - Batch processing should have independent context per document. Accumulated context from previous extractions degrades performance.
________________________________________
Q357: Your extraction system must escalate when:
A) Any extraction has low confidence B) Extraction confidence is below calibrated threshold (determined from validation set) OR document contains patterns system hasn't seen before C) Manual review is requested D) Extraction takes too long
Answer: B - Escalate based on: calibrated confidence thresholds (from validation data) or novel patterns outside training distribution. Data-driven escalation criteria.
________________________________________
Q358: Your extraction system processes invoices with embedded images (company logos, signatures, stamps over text). These complicate extraction. How should this be handled?
A) Reject documents with images B) Image-aware extraction: detect images, extract around them, note image presence in metadata: {"invoice_number": "INV-123", "has_stamp": true, "confidence": 0.85} C) Remove images first D) Extract images separately
Answer: B - Image-aware extraction: handle documents as-is, note image presence in metadata, adjust confidence accordingly.
________________________________________
Q359: Your extraction validation reveals common patterns of errors (dates in wrong format, amounts missing currency). How should this inform the system?
A) Ignore patterns B) Error pattern analysis → update validation rules → improve retry prompts with specific guidance on common errors C) Hope for improvement D) Manual correction only
Answer: B - Error pattern analysis informs system improvement: enhanced validation rules, better retry prompts targeting common errors.
________________________________________
Q360: Your extraction system provides field-level confidence scores for routing to human review. How should confidence thresholds be determined?
A) Fixed threshold (e.g., 0.8) for all fields B) Field-specific and document-type-specific thresholds calibrated using labeled validation data: analyze accuracy by field and document type, set thresholds that achieve target precision C) Random thresholds D) User preference
Answer: B - Calibrated confidence thresholds: field-specific (some fields harder than others), document-type-specific (handwritten vs printed), data-driven from validation set analysis.

